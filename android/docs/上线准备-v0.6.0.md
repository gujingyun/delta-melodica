# 安卓 v0.6.0 上线准备

渠道：官网直接提供 APK 下载。当前状态：2026-09-14 已正式发布，用户已确认真机试奏正常。

官网首页首屏与底部各有一个「下载 Android 版」入口。[安卓下载页](https://aiygzn.top/melodica/android.html)与正式 APK 已经公开 HTTPS 下载、SHA-256 和签名校验。服务器备份：`/var/backups/delta-melodica/android-v0.6.0-8bljnnz0`；本机发布记录：`work/android-release-v060-publish/`。完整对外说明见 [发布结果](../../docs/android-v0.6.0-release.md)。

## 已完成的开发准备

- 包名 `top.aiygzn.melodica`，版本 `0.6.0`／6，Android 8.0+，目标 API 34。
- 公开曲库兼容 MIDI 与 `format: score` 的 JSON 曲谱；下载先检查大小、SHA-256，再解析完整毫秒音符。
- 主界面、账号页均可打开离线「权限与数据说明」，说明游客、注册继承、主动同步及卸载行为。
- 独立正式签名，密钥和加密密码仅保存在仓库外；Release 构建检查签名和调试标记，自动输出 APK、校验值和构建来源。
- Debug 与 Release 单元测试各 28 项通过；Release Lint 0 错误、23 警告（平台建议、文字国际化等）。
- 2026-09-14 公开 HTTPS 回读：官网目录有 5 首 MIDI、1 首 JSON 曲谱；账号服务 `/health` 返回 `{"status":"ok"}`。未发送验证码邮件、未访问个人账号。

## 升级与数据迁移

旧预览包由 Android Debug 证书签名。v0.6.0 起的正式包使用新的固定证书，同包名异签名不能直接覆盖安装。旧包中先同步曲谱，登录官网账号页确认曲目可下载；没有同步的曲谱需保留原始文件。再卸载预览包、安装正式包并重新校准。不同手机厂商可能显示不同的签名冲突文案。不要让用户以清除数据或卸载作为无条件的第一步。

## 真机确认与后续维护

1. 用户于 2026-09-14 确认真机试奏正常并授权发布。用户未提供机型、系统版本及逐项测试记录，不扩大为所有手机的兼容结论。
2. 正式签名资料仍需另行制作独立加密备份并验证可恢复。当前密码文件使用本机 Windows 用户 DPAPI 保护，不是跨电脑备份；不能因本次发布而重新生成签名。
3. 官网已发布 APK 和公开网页，官网文件在 `codex/android-website-release` 分支提交并推送。2026-09-14 用户另行授权公开安卓 app 及相关代码，源码、构建脚本、测试和文档整理到 `codex/android-app-source` 分支；正式签名资料继续仅保存在仓库外。

本记录不把自动化测试或模拟器演奏结果当作手游兼容结论。应用商店上架不在本次准备范围内。

## 最终成品与设备回归

- APK：`dist/delta-melodica-android-v0.6.0.apk`，67,932 字节。
- SHA-256：`c08a157e1e72f276961dfdfaf66c86859a50c8e474804de75dc039989c9fbb39`。
- 正式证书 SHA-256：`fbb496e1dfe34cbaefe4e07fc6ab82480ca1b52807c5d86ac7fa49aa1231154c`。
- 源码构建基线：`fe6ece010b8abc931b5d795c0aa6dbde5640697f`，构建时源码干净；后续改动仅涉及验证入口、发布材料和记录，生产源码未变。
- 独立 Android 14／API 34 模拟器全新安装成功。APK 签名校验通过、不可调试、仅申请 INTERNET；演奏服务受 `BIND_ACCESSIBILITY_SERVICE` 保护，无设备测试入口或 `WRITE_SECURE_SETTINGS`。
- 15 项实际触摸回归通过：12 点校准、倒计时、音区与半音、长音和重复音、暂停续播、停止释放、禁用收起、目标应用与旋转保护、切出暂停释放。
- 5 项授权回归通过：停止隐藏保留授权、重开恢复、不重复申请、连接中展示、系统设置入口及撤销后按需申请。
- 时序回归 2 项通过，包含二倍速密集跨音区半音不丢音、完整音符和无叠按。连续跨音区测得累计延迟 464 ms，带休止示例累计延迟 4 ms，二倍速密集变音累计延迟 255 ms；为模拟器实测，不能据此承诺零延迟或手游表现。
- 5 项公开曲库回归通过：实际下载官网当前 6 首曲谱、大小和 SHA-256 校验、解析、搜索分页、取消、选用和去重、返回页面后的旧回调保护。
- 账号设备回归通过：真实 Android Keystore 加密恢复、游客归属断网重试、原文件保留、重复同步去重、账号隔离及下载；使用假服务与隔离数据，未发送真实邮件。
- 主界面、账号注册页、离线数据说明入口及截图通过。截图见 `release-main-v0.6.0.png` 与 `release-data-information-v0.6.0.png`。
- 第一轮触摸回归前 14 项通过，最后一项被新模拟器的 `ImmersiveModeConfirmation` 全屏教学提示抢走焦点。诊断确认屏幕与校准一致、键盘存活，但实际前台是 `android`，应用正确阻止发键。仅在独立测试模拟器完成该系统提示后，整组 15 项全部通过；未削弱应用前台保护，失败记录保留在 `work/android-release-v060/`。
- 官网候选两页在 320、390、768、1440 像素下无横向溢出，图片与站内链接正常，无页面脚本错误；通过实际 HTTP 下载核对 APK 大小和 SHA-256。已检查桌面与手机整页截图。
- 构建 APK、从模拟器回读的已安装 APK、官网暂存 APK 三者哈希完全相同。

## 本地预览与交付

```powershell
# 由已验证的 Release 包生成官网暂存文件，不上传服务器。
.\android\stage-release.ps1
.\.venv\Scripts\python.exe -m http.server 8767 --bind 127.0.0.1 --directory .\releases\android-v0.6.0\site
# 另一个终端中指定本机已安装的 Playwright 模块后检查页面。
$env:PLAYWRIGHT_MODULE = '本机 Playwright 模块的完整目录'
node .\android\test_release_site.cjs
```

预览地址：`http://127.0.0.1:8767/android.html`。交付目录：`releases/android-v0.6.0/`，上传范围仅限其中 `site/`。构建日志、截图和设备结果保存在本机 `work/android-release-v060/`。

## 发布与回滚步骤（本次已执行发布，保留操作参考）

1. 本次已取得用户真机试奏确认及正式发布授权，发布的是上面记录 SHA-256 的原候选包，没有重新构建替换用户验证过的成品。
2. 重新读取线上首页与目录，备份即将改动的首页、安卓页面和 Nginx 配置。2026-09-14 SSH 只读核对主机名正确，官网与 downloads 目录为 `root:root 0755`；使用现有 sudo 写入，不放宽目录权限。
3. 仅上传 `site/downloads/` 中该版本 APK 与校验文件，以及安卓 CSS 和截图；远端回读校验。不得把本机 songs.json、Windows version.json、账号数据、密钥、测试 APK、源码或完整工作目录上传。
4. 发布 `android-data.html`、`android.html`、`android-version.json`。安卓使用独立版本清单，保留 Windows 更新和下载入口。当前服务器 MIME 表未找到 APK 映射；可把 `release-site/nginx-android-download.conf` 的精确下载 location 合并到现有 HTTPS server，通过 `nginx -t` 后平滑 reload。
5. 在重新读取的线上首页下载区增加指向 `android.html` 的「下载 Android 版」入口，不用旧首页整页覆盖。确认手机和桌面布局。
6. 通过公开 HTTPS 下载 APK，核对上面的 SHA-256、字节数和 APK MIME；打开安卓两页和截图、版本清单，同时复查 Windows 下载、公开曲库和账号健康接口。
7. 需要回滚时先撤下新增首页入口并恢复已备份页面与配置；保留版本化安装包以便排查。此次是首个安卓正式下载版本，无旧安卓正式版可自动降级；已安装的用户不得被指引直接卸载丢失数据。

第一阶段本地提交已完成。当时向 `origin/codex/android-release-v060` 的推送被本机 `.git/hooks/pre-push` 拒绝，原因是安卓源码仅限本地；该阶段保留了钩子。2026-09-14 用户另行明确授权提交 app 相关代码，本次据此解除安卓源码的本地推送限制并保留原钩子备份，签名密钥和个人数据仍不入库。

正式发布阶段的公开网页提交已推送至 `origin/codex/android-website-release`，不包含安卓工程。首次网络请求出现 GitHub 443 SSL 波动，确认本机 Clash/Mihomo 和 Git 本地代理后重试成功，未改动 VPN 配置。
