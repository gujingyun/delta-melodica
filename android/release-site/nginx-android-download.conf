# 合并至现有 HTTPS server 的 include 目录，保留 Windows 下载与其他站点配置。
location = /melodica/downloads/delta-melodica-android-v0.6.0.apk {
    root /usr/share/nginx/html;
    types { }
    default_type application/vnd.android.package-archive;
    try_files $uri =404;
    add_header Content-Disposition "attachment" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Cache-Control "public, max-age=3600" always;
}

location = /melodica/downloads/delta-melodica-android-v0.6.3.apk {
    root /usr/share/nginx/html;
    types { }
    default_type application/vnd.android.package-archive;
    try_files $uri =404;
    add_header Content-Disposition "attachment" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Cache-Control "public, max-age=3600" always;
}

location = /melodica/android-version.json {
    root /usr/share/nginx/html;
    try_files $uri =404;
    default_type application/json;
    add_header Cache-Control "no-store" always;
    add_header X-Content-Type-Options "nosniff" always;
}

location = /melodica/android.html {
    root /usr/share/nginx/html;
    try_files $uri =404;
    add_header Cache-Control "no-store" always;
    add_header X-Content-Type-Options "nosniff" always;
}

location = /melodica/android-data.html {
    root /usr/share/nginx/html;
    try_files $uri =404;
    add_header Cache-Control "no-store" always;
    add_header X-Content-Type-Options "nosniff" always;
}
