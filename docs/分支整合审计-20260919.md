# 分支整合审计 · 2026-09-19

## 结论与本轮范围

此前发布的安卓 0.6.3 未包含 `codex/android-feature-sync` 的三页布局、原谱编辑、试听及片段编排。该分支与正式发布线从不同历史继续开发，版本号增加并不代表包含全部功能。

本轮以 `master` 的 `0a966f0` 为基线，真正合并 `codex/android-feature-sync`（`045c418`），版本设为 0.7.0／11。保留正式版五首内置曲目、JSON 下载、错误脱敏、三秒半音提示、权限说明、签名及构建检查；整合当前工作区的检查更新、紧凑播放条、可展开悬浮曲库和 5000 首目录上限。官网部署不在本轮范围。

合并时补齐五首内置曲目的原谱编辑、官网下载目录标题与编辑元数据兼容、搜索结果普通链接标题回退，并修复服务同实例重连后的销毁状态。新增回归验证这些交叉路径。

## 其它真实遗漏

| 分支 | 未合并内容与证据 | 本轮处理 |
| --- | --- | --- |
| codex/android-tone-smooth | `6183b22f` 切调衔接，ToneTiming；`d5e08834` 验证记录 | 保留分支，尚未移植至正式线 |
| codex/android-gap10-probe | `a637b60b` 10ms 切换实验；记录显示短点击错音 | 不合入正式版 |
| codex/android-gap32 | `3489686f` 快速切调模式，`cb205730` 验证记录 | 保留分支，需要独立取舍和真机验证 |
| codex/android-service-recovery | `fa420fac` 前台后台保护，`d99a2e27` 权限与重连文案等 | 后台保护未合入；仅摘取同实例重连状态修复并测试，不增加前台服务权限 |
| codex/server-public-library-upload | `6cff711` 公开曲库上传接口，比原 master 多一笔；根工作区还有服务和网站草稿 | 进行中的工作，保留其提交和草稿，不并入本轮安卓主线 |

以上安卓后续分支也继承了本次三页界面分支；其共同祖先部分不再算遗漏，表中列的是各自额外功能。

## 看似遗漏、实际已移植

不能只看 `git branch --no-merged` 或提交号。对历史提交和主线对应提交逐文件对照，以下应用代码一致，差异主要是 README、验证记录或忽略规则：

| 历史提交 | 主线对应提交 | 内容 |
| --- | --- | --- |
| b7c2c8aa | 6a3ba91 | 简谱保存按钮布局 |
| 1a4ea4b2 | d78a2b4 | 邮箱账号与 SQLite |
| 60ce49f1 | e37b9fe | Windows 0.15 发布 |
| a916a092 | e763ab5 | 分平台下载统计 |
| 00478660 | ba88744 | 统计上线记录 |
| 855a07e9 | 059a34a | 图片谱面 |

`codex/android-app-source` 的 `e9ff1f4` 与主线 `fe6ece0` 对照无应用源码差异，差异为文档、发布和测试材料；不能当作另一套遗漏的安卓主界面。

## 核对范围

已刷新远程引用，检查 31 个本地分支、提交 ancestry、patch 对应关系和差异文件；检查既有工作区，仅根工作区存在未提交内容。以下为整合前审计分类，后续安卓分支仅“共同祖先”被本次合并，额外功能仍见上表。

| 分支 | 核对结果 |
| --- | --- |
| codex/accounts-cloud | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/aggregate-midi-search | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/android-app-source | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/android-feature-sync | 本轮合并 |
| codex/android-gap10-probe | 有额外安卓功能未移植，见上表 |
| codex/android-gap32 | 有额外安卓功能未移植，见上表 |
| codex/android-prototype | 已由基线包含 |
| codex/android-release-v060 | 已由基线包含 |
| codex/android-service-recovery | 有额外安卓功能未移植，见上表 |
| codex/android-tone-smooth | 有额外安卓功能未移植，见上表 |
| codex/android-unified | 本轮整合工作分支 |
| codex/android-website-release | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/audio-transcription | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/builtin-unlock-songs | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/fix-jianpu-save | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/fix-online-score-download | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/homepage-hls-video | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/ignore-local-agent-notes | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/interface-refresh | 已由基线包含 |
| codex/interface-refresh-preview | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/jianpu-search | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/new-score-workbench | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/platform-download-stats | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/precise-score-preview | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/release-v0.13 | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/release-v0.14 | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/release-v0.15 | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/score-editor-desktop | 功能已包含或有对应移植，历史提交与记录不同 |
| codex/score-editor-preview | 已由基线包含 |
| codex/server-public-library-upload | 当前工作分支，服务器功能进行中 |
| master | 已由基线包含 |

## 验证

- Android 单元测试共 54 项：53 通过、1 项线上搜索测试跳过；不访问生产服务。
- FeatureChecks：竖屏、720×1280 软键盘输入、1280×720 横屏通过，包含三页导航、每曲设置、片段、原谱编辑、试听跟随与搜索布局。
- AccountChecks：隔离目录及假服务，Keystore 加密恢复、游客继承断网重试、账号隔离、参数隔离、云端下载及离线权限说明入口通过。
- 新界面截图：[主界面](../android/docs/main-v07.png)、[软键盘编辑](../android/docs/editor-keyboard-v07.png)。截图使用自编测试谱及专用模拟器。
- GestureSmokeTest 完整 17 项通过：校准、重复音、休止、长按、暂停／续播／停止、定位释放、倒计时取消、变音、密集短音、详情窗、目标／旋转保护、切出释放和同实例重连；`INSTRUMENTATION_CODE: -1`。
- 小屏悬浮布局：1280×720、密度 2.0、字体 1.3，暂停条 333×112，选曲、播放收起、暂停恢复、停止、拖动及边界 3 组验证通过。
- 权限生命周期 5 组通过：隐藏保留授权、重开不重复申请、已授权等待连接、管理授权、未授权按需申请。
- 专用模拟器首次全屏引导曾抢占焦点，应用正确拒绝发键；关闭设备引导后完整复测通过。
- 正式清理构建通过：单元测试、Release Lint（0 错误／47 警告）、签名校验及不可调试检查。APK 来源提交 `77d4e51e2cf90a7831749ede8fa3e0a9b90424de`，`sourceDirty=false`。
- 本轮不把模拟器验证当作手游真机试奏。

## 本地正式签名包

文件：`dist/delta-melodica-android-v0.7.0.apk`，315899 字节，版本 0.7.0／11，未部署官网。

SHA-256：`9dfb11afe0d5a4a21f12cd672d6c56503412b9604d3c49a2022dcdb0f7d110a3`。

证书 SHA-256：`fbb496e1dfe34cbaefe4e07fc6ab82480ca1b52807c5d86ac7fa49aa1231154c`，与此前正式包相同，可覆盖升级。APK 和构建元数据仅保存在本地 dist，不进入 Git。

回退时优先从合并前提交 `0a966f0` 构建新的更高版本号正式包；不要指导用户卸载后降级，以免丢失本地曲库。

## 工作区与发布保护

根工作区原有 7 个安卓改动文件已按 SHA-256 备份，整合时保留其功能；其它服务器、网站和迁移归档草稿不纳入提交。发布签名留在仓库外，不提交密钥、真实服务地址、账号数据或归档。

本轮产物与官网旧版本分开标识，后续发布必须从包含本次合并的主线构建，避免再次由旧发布分支直接提升版本号。
