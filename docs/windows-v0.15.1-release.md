# Windows v0.15.1 发布记录

2026-09-14 按用户要求将审查发现的五项缺陷修复发布到官网。

## 发布内容

- [官网下载](https://aiygzn.top/melodica/)和程序内检查更新均指向 v0.15.1；[更新说明](https://aiygzn.top/melodica/updates-v0.15.1.html)介绍退出草稿保护、BOM 曲谱兼容、旧版 JSON 导入、损坏曲目删除和游客继承记录恢复。
- [正式成品](https://aiygzn.top/melodica/downloads/delta-melodica-v0.15.1.exe)为 22,415,900 字节，SHA-256：`2ea5bc0ca939e282333ff47eb0657a6b042089c11fa84056d9046b18aafba29a`。本机文件为 `dist/三角洲口风琴_v0.15.1.exe`，PE 清单保留 `requireAdministrator`、`uiAccess=false`。
- 升级前从系统托盘退出旧版，再启动新版；继续沿用原有本地曲库和设置。游客继承记录损坏时保留原记录，恢复已有备份并重启后可继续原批次。
- 发布源码及页面提交为 `fcd51dd`，已推送 GitHub 分支 `codex/android-release-v060`。

## 验证结果

- 最终版本完整回归 **266 项通过**，覆盖曲谱、调度、编辑器、账号、下载、更新、设置、退出和异常恢复。
- 正式 EXE 和公网重新下载的 EXE 均在独立数据目录通过 `--smoke`，退出码 0；两份 BOM／旧格式样例实际解析成功，`score_json_tested=2`，损坏游客继承记录保持原始内容。编辑器、托盘隐藏恢复和搜索入口通过。
- 仅在隔离冒烟子进程使用 `RunAsInvoker`；交付文件的管理员清单保持原样。未使用个人曲谱或真实账号，未向游戏发送按键。
- 官网首页与更新说明在 320／390／768／1440 px 检查通过，下载入口、图片、锚点、键盘访问、视频延迟加载和错误重试正常。已检查[新版主窗口截图](../website/assets/main-window-v0.15.1.png)、桌面首页及手机更新说明。
- 5 个发布文件经过公开 HTTPS 完整下载，字节数和 SHA-256 与上传清单全部一致。官网下载接口返回 302 至新版 EXE，`version.json` 带 `no-store`，更新解析结果为 `(0, 15, 1)`，高于 v0.15。
- Nginx 配置检查通过；发布后 Nginx、统计和账号服务均为 active，公开健康检查正常。私有账号与统计接口仍返回 401，既有视频清单和 v0.15 旧包可访问。

## 发布与备份

- 使用限定的五文件清单、独占发布锁、当前版本及首页／版本清单／统计源码哈希检查。先发布安装包、截图、更新说明，再切换统计服务下载目标、首页和版本清单；异常时由发布脚本恢复原文件。
- 服务器备份位于 `/var/backups/delta-melodica/v0.15.1-79qj1fbj`，包含切换前的首页、版本清单、统计服务源码以及文件清单。旧安装包保留，官网目录权限仍为 `root:root 0755`，新版 EXE 为 `root:root 0644`。
- 发布前后线上 `songs.json`、安卓页面与版本清单、账号页面／脚本／样式和公共样式文件哈希一致。曲库仍为 7 首，没有覆盖线上新增曲谱。
- 本机构建、部署清单、服务端结果、公网校验和两次成品冒烟记录位于已忽略的 `work/release-v0151/`。
- 本次仅发布 Windows 客户端修复与配套官网内容；游戏内实机演奏和 Android 设备测试未执行。
