# 安卓 0.7.2 官网发布

2026-09-20 发布至 https://aiygzn.top/melodica/android.html 。

从线上 0.6.3 升级到 0.7.2（versionCode 13），包含三页主界面、原谱编辑和试听、最终约 32 毫秒快速切调试用方案，以及播放前校准提示。默认仍使用稳定切调。官网使用正式 0.7.2 的实际主界面截图。

## 产物

- 文件：`delta-melodica-android-v0.7.2.apk`，317843 字节。
- SHA-256：`6d6d0d6a1a898340883d45454d1f6d1425b71dc32ada18e6fac8d38e30678a53`。
- 正式签名 SHA-256：`fbb496e1dfe34cbaefe4e07fc6ab82480ca1b52807c5d86ac7fa49aa1231154c`。
- 复用已验证成品，源码提交 `e71ec7b3b58446ce3692cb55e185de708f077a39`；未重新构建。

## 发布与验证

- 本地两页、四档宽度（320/390/768/1440）检查通过：无横向溢出、图片和站内链接正常、APK 大小与校验值匹配。
- 统计服务在服务器临时目录使用隔离数据运行 7 项测试，全部通过。
- 使用发布锁并校验线上基线；备份后先上传 APK、校验文件和主界面图片，更新 Nginx 与统计下载目标，最后原子替换安卓页面和清单。
- 迁移后的 Nginx 配置位于 `/etc/nginx/default.d/delta-melodica.conf`，只追加安卓下载、页面与清单的精确规则；保留已有后台、账号、曲库和限流配置。
- Nginx 配置校验和服务状态通过，Windows 下载目标及原网站文件哈希保持一致，实时统计数据未被覆盖。
- 公网版本清单、安卓 HEAD 下载跳转、APK MIME、大小、SHA-256 与页面字节一致性通过。

## 回滚

备份目录：`/var/backups/delta-melodica/android-v072-y8sh788n`。需要回滚时，持有 `/var/lock/delta-melodica-release.lock`，恢复备份中的安卓页面、版本清单、Nginx 合并配置和统计服务代码；运行 `nginx -t` 后 reload，并重启统计服务。保留新旧安装包，不覆盖实时统计数据。

原始日志与候选文件保存在本机忽略目录 `work/android-release-v072/`，不包含在 Git 提交中。

用户确认服务器限制为中国地区访问，公网浏览器验收受该限制，未取得线上浏览器截图；本次保留地区访问限制。HTTP 回读页面与已通过布局检查的本地候选页面逐字节一致，公网 APK 另用 curl 独立下载后哈希一致。以下为本地候选页面截图。

[手机页面](android-v0.7.2-390.png) · [桌面页面](android-v0.7.2-1440.png)
