# 验证记录

## 2026-09-14：安卓 v0.6.3 官网发布

- 使用已验证的正式成品，未重新构建：版本 0.6.3、versionCode 9、69,292 字节，SHA-256 为 `59552ed90bbd01efc114598d06b116f093a1a8e873c96045cdd3d9f480093dd9`。本轮再次核验 APK 签名、包名、最低系统和版本信息。
- 暂存下载页、版本清单、APK 和 SHA 文件，更新安卓下载跳转及 Nginx 精确下载规则。Linux 临时目录内运行 `python3 -m unittest -v server.test_melodica_stats`，7 项全部通过，测试数据与线上数据隔离。
- 本机 Chrome 检查安卓下载页和权限页，320／390／768／1440 px 四档宽度无横向溢出；图片、站内资源、锚点、APK 大小与 SHA-256 正常，页面无脚本错误。已检查手机与桌面截图。
- 持有发布锁并校验旧文件基线后部署，备份位于 `/var/backups/delta-melodica/android-v063-bi16fqsl`。`nginx -t` 通过，Nginx 平滑 reload，统计服务重启后与账号服务均为 active。保留首页、Windows 清单与成品、曲库索引、账号资源、旧 APK、样式和教程资源的哈希一致，实时计数未被覆盖。
- 公网 HTTPS 页面及清单与本地文件逐字节一致；Chrome 实际点击一次安卓下载，下载文件名、大小、SHA-256 正确，APK MIME 正确。此次验收期间安卓计数从 46 增至 47，Windows 保持 224；两端 HEAD 跳转正确，后台和未登录私有曲库仍返回 401，健康检查为 200。
- 官网已由 v0.6.0 切换至 v0.6.3；发布过程与截图见[发布记录](docs/android-v0.6.3-release.md)。本次发布不增加手游真机验证结论，客户端验证范围仍为下述模拟器检查。

## 2026-09-14：安卓 v0.6.3 自动消失的半音文字提示

- 删除「开始演奏」「取消」及额外确认区。点击播放／继续后，紧凑控制条内显示「半音设为未选中」，同时准备倒计时 3 秒；到时文字自动换回播放进度并演奏。提示沿用控制条，不弹系统通知、不改变窗口高度。
- 三秒准备期间不预选变音，也不发送音键，留给用户关闭半音；结束后再次检查前台和音键遮挡，再以半音关闭初态演奏。暂停、停止、隐藏、拖动暂停、切出或旋转都会取消延迟任务，续播保留原进度并重新提示。
- 28 项 Release 单元测试通过，Lint 为 0 错误、25 警告。1280×720 小屏横屏、密度 2.25、1.3 倍字体的设备布局检查通过，覆盖真实点击播放、提示自动消失并演奏、暂停／停止、隐藏取消延迟演奏、拖动和边缘恢复。提示完整且无新增按钮，准备与演奏状态均为 493×126 px；已查看[准备提示](android/docs/half-toast-v063.png)及[提示消失后的演奏](android/docs/half-text-playing-v063.png)截图。
- 最终成品从干净提交 `7ea4cd2` 构建：`dist/delta-melodica-android-v0.6.3.apk`，69,292 字节，SHA-256：`59552ed90bbd01efc114598d06b116f093a1a8e873c96045cdd3d9f480093dd9`。正式签名与 v0.6.0～v0.6.2 一致，可覆盖升级，APK 不可调试。
- 最终 APK 的完整触摸回归 15 项通过，返回 `INSTRUMENTATION_CODE: -1`，包含提示期间停止取消、自然音／半音自动演奏、准备阶段不发手势、暂停续播、旧变音回调失效、停止归零及目标窗口／旋转／切出保护。模拟器已安装 APK 回读与交付文件 SHA-256 一致。
- 测试限独立 Android 14 模拟器自建键盘，未连接真实手机、未进入手游；源码与记录按阶段提交推送，官网下载仍为 v0.6.0。

## 2026-09-14：安卓 v0.6.2 半音关闭提示

- 删除悬浮窗「已选中／未选中」两个状态按钮，播放和续播前统一提示用户将游戏内半音设为未选中，再点「开始演奏」；保留取消入口。内部仅按半音关闭初态准备演奏，后续仍按乐谱切换半音和音区。提示期间不会读取、自动关闭半音或发送演奏手势。
- 更新相关提示、测试键盘说明与设备测试：验证旧选择按钮已消失，初始半音开启时提示期间保持原样，用户手动关闭后真实点击开始得到正确自然音与半音；续播／中断后仍提示，停止可以取消等待布局的开始请求。
- Release 构建、28 项单元测试通过，Lint 为 0 错误、25 警告。完整触摸回归 15 项通过；1280×720、密度 2.25、1.3 倍字体下的悬浮布局及真实暂停／停止／拖动检查通过。已查看[半音提示截图](android/docs/half-prompt-v062.png)及小屏截图，提示和开始／取消按钮完整。
- 从干净提交 `6ca1115` 重新构建正式签名 APK；与完整回归所用构建比较，APK 内全部文件条目内容逐字节相同，包含代码、资源及清单。最终交付 `dist/delta-melodica-android-v0.6.2.apk`，69,964 字节，SHA-256：`d82b105bb540b4db555990c99dad78cb1b2e1a824964d6a7f0fcd205c5bf4e0e`；正式证书与 v0.6.0／v0.6.1 一致，可覆盖升级。
- 最终 APK 安装后再次通过小屏大字体悬浮窗回归，返回 `INSTRUMENTATION_CODE: -1`；模拟器回读的已安装 APK 与交付文件 SHA-256 一致。
- 测试仅使用独立 Android 14 模拟器的自建键盘；未连接真实手机、未进入手游。源码与验证记录按阶段提交推送，官网下载仍为 v0.6.0。

## 2026-09-14：安卓 v0.6.1 自适应悬浮控制条

- 取消固定窗口长宽，按可用屏幕短边、实际字体和按钮最小点击面积计算宽度，高度随内容撑开。倒计时／播放自动隐藏曲名、说明、校准与收起，保留进度、暂停、停止和拖动；暂停／停止／结束释放触摸后恢复操作。
- 拖动主动暂停，手指按住时保持布局；靠边展开收回屏幕内。修复半音确认区收起后仍用旧高度检查遮挡的问题，停止或新确认使旧布局回调失效。
- 干净提交 `82f6ff6` 的 Release 构建、28 项单元测试和 Lint 通过（0 错误、25 警告）。最终 APK 的完整 15 项自建键盘触摸回归通过；普通横屏、小屏横屏及 1.3 倍系统字体下的布局、真实暂停／停止、长按拖动和边缘检查通过，截图已检查。实测播放占用约为普通状态的 44%～51%，尺寸随屏幕和字体变化。
- 本地 APK `dist/delta-melodica-android-v0.6.1.apk`，70,156 字节，SHA-256：`9474a9c5284ea8c99646eb04ba77b3093ff756f906fd0caf9deba05cb09682df`。签名与 v0.6.0 正式版一致，可覆盖升级；从模拟器回读的已安装包哈希一致，正式包不可调试。
- 源码已按阶段提交并推送；官网仍为 v0.6.0。测试限独立 Android 14 模拟器和自建键盘，未进入手游，不代替真机显示与听感验证。详见 [v0.6.1 悬浮窗记录](android/docs/悬浮控制条-v0.6.1.md)。

## 2026-09-14：官网下载分平台统计

- 新增 Android 下载计数入口，后台分别显示 Windows、Android 及下载总次数；Windows 历史次数、浏览次数和创建时间保留。安卓页面和发布模板均使用新入口。
- Linux Python 3.6.8 隔离回归 7 项通过，包含旧数据兼容、首次写入迁移、HEAD 不计数和 60 个混合并发请求。后台在 320／390／768／1440 px 验证通过，测试截图已检查；浏览器本地模拟重定向下载的文件名及内容校验通过。
- 已部署官网；公开 HTTPS 回读安卓页面哈希一致，实际点击一次下载后 APK 文件名、大小和 SHA-256 核对通过。验收时 Windows 186 次保持不变，Android 0 → 1，总计 186 → 187；安卓的 1 次为验收下载。HEAD 不计数，私有统计后台与接口仍返回 401。
- 服务器备份为 `/var/backups/delta-melodica/platform-stats-_ykypw45`。仅重启统计服务，Nginx 和账号服务正常；原安装包、版本清单、首页与曲库等保留文件哈希一致。统计是下载入口 GET 请求次数，Android 历史静态下载不回填。详见[分平台统计记录](docs/platform-download-stats.md)。

## 2026-09-14：Windows v0.15.1 正式上线

- 将五项审查缺陷修复纳入正式 v0.15.1，更新客户端版本、官网版本清单、首页截图、更新说明和 Windows 下载目标。
- 最终版本完整回归 266 项通过；首页和更新说明在 320／390／768／1440 px 检查通过，主窗口、桌面首页和手机更新说明截图已检查。
- 正式成品 `dist/三角洲口风琴_v0.15.1.exe` 为 22,415,900 字节，SHA-256：`2ea5bc0ca939e282333ff47eb0657a6b042089c11fa84056d9046b18aafba29a`。保留管理员权限清单；隔离成品冒烟通过，BOM 与旧格式曲谱均实际解析，损坏游客记录保留不变。
- 发布前核对线上为 v0.15，首页、版本清单、公共样式和统计服务源码与本地基线哈希一致；Nginx 配置检查通过，Nginx、统计与账号服务正常。
- v0.15.1 已正式上线，5 个公开文件通过 HTTPS 完整回读校验；下载接口和程序更新提示均切换到新版。公网下载的 EXE 再次通过隔离冒烟，旧版包仍可下载，既有账号、安卓页面和 7 首线上曲谱保持原状。
- 服务器备份为 `/var/backups/delta-melodica/v0.15.1-79qj1fbj`；详细测试范围、发布顺序与校验结果见 [Windows v0.15.1 发布记录](docs/windows-v0.15.1-release.md)。本次未进行游戏内发键验证。

## 2026-09-14：审查缺陷修复第二阶段与本地成品

- 编辑器关闭、托盘退出和备用退出按钮统一提供保存、放弃、取消；应用只在编辑器确认关闭后设置退出状态。确认框前释放主播放器和试听输出，取消退出后继续处理队列，拦截重复退出和确认期间的播放请求。保存失败时窗口及草稿保留。
- 游客继承记录读取或结构校验失败时保留原文件并显示恢复提示，禁止自动恢复和重新归属；仍可打开本地曲库、登录及同步账号专属曲谱。有效记录中的副本恢复失败也不会阻止启动，可重试原批次。恢复备份后重启能够使用原批次令牌继续继承。
- 新增退出保存/取消/失败/备用入口、损坏归属记录、原批次恢复和本地界面可用性回归；原来假定静默退出的测试改为明确选择放弃，测试清理使用内部强制关闭。
- 完整回归 `python -m unittest -q test_music test_jianpu_rules test_jianpu_search test_score_editing test_source_editor test_image_score_download test_account_client test_accounts test_resource_search test_update test_online_library test_song_settings test_score_editor test_resource_search_ui test_transport test_lifecycle test_score_files`：**266 项通过**。账号提示高度调整后，`test_account_client test_song_settings test_lifecycle` **46 项再次通过**。
- 源码隔离冒烟通过。冒烟入口新增对目录中全部 JSON 的实际解析和演奏计划检查；BOM 与旧版两份生成曲谱检查通过。没有使用个人曲谱或真实账号。
- 真实 Windows 确认框截图与取消验证通过；仅向本次测试进程的确认框发送取消命令，没有发送全局键鼠。账号页初次截图发现恢复提示挤压返回按钮，已在显示恢复提示时增加高度，回归及重拍确认完整可见。截图：[退出确认](docs/audit-fix-exit-confirmation.png)、[账号恢复](docs/audit-fix-account-recovery.png)。
- 通过 PyInstaller 6.11.1 重新构建 `dist/三角洲口风琴_审查修复版.exe`，22415889 字节，SHA-256：`6eb48243de82e2dcfb12f852c401a173e01f35fdf340c688415ee92fac5b647c`。保留管理员权限清单，未改变公开版本号与官网下载包。
- 最终 EXE 使用独立目录及 `--smoke` 通过，退出码 0、`score_json_tested=2`；验证曲谱解析、编辑器、托盘隐藏恢复、搜索入口，以及损坏归属记录下的启动。仅在此无发键冒烟进程设置 `__COMPAT_LAYER=RunAsInvoker`，无需交互提权；成品正常启动的清单保持原样。原损坏记录回读未变。结果与本地构建日志位于 `work/audit-fixes-20260914/`。
- 未向游戏发键，未运行 Android 设备或 Windows 实际键鼠测试；未部署官网、后端或发布新安装包。原审查的五项缺陷已修复，其余架构和功能扩展仍为后续工作。

## 2026-09-14：审查缺陷修复第一阶段

- 新增 `score_file.py`，集中曲谱 JSON 的 BOM 兼容、大小限制、格式识别和导入附注校验；主界面、编辑器、同步器与下载复用此入口。旧版文本 JSON 可直接重新导入，未知版本不会回退为旧格式。
- 删除入口按曲库列表选中的文件启用，曲谱读取失败后仍可删除副本；保留取消确认、路径边界校验和每曲设置清理。
- 新增 `test_score_files`：修改前 BOM 导入/下载载入、旧格式导入和损坏条目删除均失败；修复后相关 5 项用例通过，覆盖重启、编辑、同步读取、源文件保留及非法格式拒绝。
- `python -m unittest -q test_score_files test_online_library test_image_score_download test_score_editor test_source_editor test_song_settings test_account_client`：85 项通过。使用临时曲库及本机假账号服务，未发送游戏按键或真实邮件。完整冒烟、截图及其余缺陷修复将在第二阶段验证。

## 2026-09-14：Windows v0.15 与用户修改的父亲上线

- Windows v0.15、官网下载入口、版本清单与更新说明已发布，5 个公开文件 HTTPS 完整回读哈希一致；正式 EXE 为 22,412,476 字节。145 项回归、源码与成品隔离冒烟、官网四档宽度检查通过。
- 用户修改的《父亲》以 80 BPM、475 音、237 秒发布，保留歌词与可编辑谱面。线上曲库由 6 首增加到 7 首，已有条目保留；实际 v0.15 成品从官网重新下载并打开谱面编辑器通过，文件哈希一致。
- 客户端发布前完成备份；安卓、账号及既有服务检查正常。未在游戏内发键；完整成品哈希、发布回读与验证范围见 [Windows v0.15 发布记录](docs/windows-v0.15-release.md)。

## 2026-09-14：图片识谱精确时值与谱面编辑修复

- 图片识谱此前为保留三连音使用纯文本精确格式，编辑时缺少谱面。原谱模式新增 `1:1/3` 等分数／小数拍数，保留调号、原级数、点击定位和试听跟随；谱面直接标明特殊时值，避免显示成普通四分音符。独立 `//` 注释行不参与演奏。
- 升音、八度、整曲移调保留精确拍数；附点按钮乘以 1.5，半拍、四分之一拍和延长按钮改用普通时值。拒绝零拍、非法分母、超出范围及与普通时值混写；源站模式拒绝本地精确拍数，防止混淆源站播放规则。
- `python -m unittest -q test_jianpu_rules test_source_editor test_score_editing test_score_editor`：54 项通过；`python -m unittest -q test_music test_transport test_jianpu_search test_image_score_download test_score_editing test_source_editor test_jianpu_rules`：140 项通过。新增回归覆盖精确拍数、反复与延音、休止、全角字符定位、选段、工具按钮、谱面及另存重开。
- 实际两页识谱曲目转换为 E4 原谱格式，559 个音符、172000 毫秒；转换前后的毫秒音高、起止时间及连奏标记全部相同。已重新导入本地曲库并打开双栏编辑器，检查普通谱面和 1/6 拍、11/8 拍等标记；个人曲谱、识别记录及截图仅保存在本地，不提交 Git。
- 源码与实际 EXE 均在独立数据目录完成 `--smoke`，退出码 0；验证了带精确拍数的曲谱、谱面、窗口最大化／还原、播放位置标记、托盘恢复及搜索入口。结果和截图位于 `work/precise-score/`。
- 本地成品 `dist/三角洲口风琴_图片谱面修复版.exe`，22412480 字节，SHA-256：`3933c9c805968811bc620580acb3ebf77ffa142f78c0c32e9f6edb4aa0910060`。成品保留管理员权限清单，仅独立冒烟子进程使用 RunAsInvoker。旧 v0.14 不支持新增拍数写法，需退出旧版后使用修复版；官网公开下载包未变更，本次未进行游戏内发键验证。


## 2026-09-14：手机版安装使用教程上线

- 按用户要求将 9 月 14 日手机教程部署到安卓下载页，首屏新增「观看安装使用教程」；保留完整画面、60 帧和原声，网页副本约 65 MB，HLS 共 30 个分片。
- MP4 与 HLS 全片解码、35 个公开文件 HTTPS 哈希、MIME、Range 206 及缺失分片 404 均通过。本地及线上原生 HLS／hls.js 实际播放、103 秒定位、重试与窄屏布局通过；MP4 兼容播放及全屏通过。
- 服务器备份 `/var/backups/delta-melodica/android-tutorial-2f06mvh9`；沿用视频配置，未重载服务，原首页、版本清单、正式 APK、曲库和账号脚本哈希保持一致。详细[教程上线记录](docs/android-install-tutorial-release.md)与截图已推送至 `origin/codex/android-website-release`，提交 `c9f9be1`、`1f22b09`；安卓发行模板和暂存脚本已同步保存在本地。

## 2026-09-14：安卓 v0.6.0 正式上线

- 用户确认真机试奏正常并授权发布，沿用其已验证的 v0.6.0／6 正式签名包，没有重新打包。机型和系统版本未提供，不补写未经确认的实机结论。
- 官网首屏与底部下载区各增加一个「下载 Android 版」入口，均进入 `android.html`；安卓 APK、校验文件、页面、截图、独立版本清单与权限说明已上线。
- 9 个公开文件均通过 HTTPS 完整回读和 SHA-256 对照。APK 为 67,932 字节，SHA-256 `c08a157e1e72f276961dfdfaf66c86859a50c8e474804de75dc039989c9fbb39`，MIME 为 `application/vnd.android.package-archive`，公开下载包 APK v2 签名与本机证书一致。
- 本地首页四种宽度回归通过，官网手机／桌面页面截图检查通过；实际点击首页入口并触发浏览器下载，文件名与哈希再次核对通过。记录位于 `work/android-release-v060-publish/`。
- 发布取得独占锁并备份至 `/var/backups/delta-melodica/android-v0.6.0-8bljnnz0`，APK 和资源先到位，配置验证后平滑 reload，首页最后原子替换。Nginx 主进程 PID 仍为 2276，统计及账号服务保持 active。
- Windows 仍为 v0.14，下载跳转、EXE 与视频入口正常；Windows 版本清单、公开曲库内容和账号脚本哈希保持原值；私有后台与账号曲库仍返回 401。没有上传源码、密钥、测试包或个人数据。
- 官网公开文件与上线记录分两阶段推送到 `origin/codex/android-website-release`。首次 GitHub 443 SSL 波动，检查本机 VPN／代理后重试成功；手机源码保护仍保留。正式签名独立离线备份继续作为维护事项记录。

## 2026-09-14：安卓 v0.6.0 官网 APK 上线准备（尚未发布）

- 修复官网 JSON 曲谱下载，沿用毫秒音符校验与本地去重；新增离线权限与数据说明、独立正式签名构建和官网暂存脚本。Windows 客户端与线上文件未改动。
- Debug 和 Release 单元测试各 28 项通过，Release Lint 0 错误、23 警告。正式包验证为版本 `0.6.0`／6、不可调试，测试入口及测试权限不混入 APK。
- 独立 Android 14 模拟器：15 项触摸回归、5 项授权回归、5 项官网曲库回归、2 项时序回归、账号隔离／Keystore／断网重试及界面检查通过，所有设备入口最终为 `INSTRUMENTATION_CODE: -1`。官网当前 5 首 MIDI 和 1 首 JSON 曲谱全部实际下载、校验并解析。账号测试使用假服务，不发送邮件。
- 初次触摸组在最后一项准备阶段被系统首次全屏提示抢占前台；诊断确认前台为 `android`，应用正确阻止发键。仅在独立测试环境完成系统教学提示后整组通过，保留失败日志，未削弱应用保护。
- 正式 APK 为 67,932 字节，SHA-256 `c08a157e1e72f276961dfdfaf66c86859a50c8e474804de75dc039989c9fbb39`；构建、设备安装回读、官网暂存三份一致。证书与干净构建基线详见 [安卓上线准备](android/docs/上线准备-v0.6.0.md)。
- 本地官网两页在 320／390／768／1440 像素宽度下通过资源、链接、布局和下载哈希检查，已检查主界面、数据说明及桌面／手机网页截图。SSH 仅检查目录权限和下载 MIME 配置，没有上传或改动服务器。
- Git 第一阶段提交 `fe6ece0` 已本地保存；推送 `origin/codex/android-release-v060` 被现有手机内容保护钩子拒绝，未移除或绕过。后续发布材料继续本地提交。仍需真实手机／手游试奏、正式签名独立加密备份与正式上线确认。

## 2026-09-13：Windows v0.14 正式上线

- 用户确认发布后，官网主页、更新说明、账号页样式、正式客户端及公开版本清单已切换为 v0.14。下载入口 `/melodica/api/download` 返回 302，目标为 `downloads/delta-melodica-v0.14.exe`；旧 v0.13 安装包仍返回 200。
- 通过公开 HTTPS 完整下载并逐个比对 8 个发布文件 SHA-256，全部与本地暂存清单一致；新版 EXE 为 22,411,500 字节，哈希 `066056fd761d84eeb4704281ee5dc046b303aa10ea330f57d8ba5561fc0a1c0c`。客户端实际读取到 0.14 且高于旧 0.13，版本清单保持 `no-store`。
- 统计与账号健康接口正常，Nginx、统计和账号服务均为 active；私有统计页、统计接口和账号私有曲库在未登录时返回 401。6 首线上曲库的索引 SHA-256 与发布前一致，账号脚本未改写；视频 HLS 清单仍可访问。
- 发布前备份位于服务器 `/var/backups/delta-melodica/v0.14-tozzze_9`，逐文件原子替换资源并在下载服务就绪后最后切换版本清单；没有改动账号数据库、凭据或现有曲谱文件。仅重启下载统计服务，未重启账号服务或修改 Nginx 配置。
- 首次公开回读脚本误用了不存在的账号路径，收到 404；按现有服务路由修正为 `/account-api/library/songs` 后验证通过。两次下载入口验证按现有统计规则共记为 2 次下载点击。结果保存在 `work/release-v014/online-verification.json`。
- 发布代码与说明分阶段推送到纯桌面 `codex/release-v0.14` 分支，保留本机手机端历史保护；未向实际游戏发键。

## 2026-09-13：Windows v0.14 发布准备

- 按用户确认的更新说明统一客户端、曲库请求标识、官网版本清单及下载目标为 0.14，发布范围为 Windows 客户端与官网主页／账号页样式；新增 `updates-v0.14.html`。正式截图显示新建简谱、聚合搜索与 v0.14，保留历史视频说明。
- `python -m unittest -q test_music test_transport test_song_settings test_score_editing test_score_editor test_source_editor test_jianpu_search test_jianpu_rules test_online_library test_image_score_download test_resource_search test_resource_search_ui test_update test_lifecycle`：222 项通过；独立运行 `test_accounts test_account_client`：28 项通过，合计 250 项。账号测试使用隔离数据与假邮件，没有发送真实邮件。
- 官网本机 `test_homepage.cjs` 与 `test_account_web.cjs` 通过：320／390／768／1440 px、下载入口与锚点、键盘访问、视频延迟加载和错误重试，以及账号端到端联调。更新说明页面另核对四档宽度、六项内容与版本号；桌面及手机截图已检查。
- PyInstaller 单文件成品 `dist/三角洲口风琴_v0.14.exe` 为 22,411,500 字节，SHA-256：`066056fd761d84eeb4704281ee5dc046b303aa10ea330f57d8ba5561fc0a1c0c`。管理员清单与 EXE 内嵌版本核对通过，程序／官网版本同为 0.14。
- 实际成品在隔离目录从官网成功下载《小金不臭不臭》，保留 44 个音符并打开原谱编辑，最大化／还原、新建工作台、搜索入口、托盘隐藏／恢复通过，退出码 0。成品冒烟仅对子进程使用 `RunAsInvoker`；未向实际游戏发键。
- 发布暂存包仅含 8 个指定文件，排除线上曲库、账号数据和配置；上线前按文件校验、备份并原子替换，最后更新版本清单。准备报告及暂存清单位于 `work/release-v014/`，实际上线结果另行记录。

## 2026-09-13：新建简谱统一使用编辑工作台

- 侧栏「输入简谱」改为「新建简谱」，复用原谱编辑工作台，提供谱文／谱面双栏、符号工具、调号与速度、撤销／重做、移调、全曲／选段试听及最大化。新建默认「我的旋律」、C 调、100 BPM 与示例谱文，不读取当前选中曲目，保存按钮显示「保存到曲库」。
- 保存使用标准毫秒曲谱并保留原谱文字；自动选中新曲，再编辑仍恢复原谱预览。无选中曲目也可新建，重复打开保留草稿，未保存关闭、非法谱文及 F9 试听停止沿用编辑器机制。
- `python -m unittest -q test_song_settings test_score_editor test_source_editor test_score_editing`：52 项通过；`python -m unittest -q test_music test_online_library.OnlineLibraryTests test_image_score_download`：70 项通过。首次合并运行有 4 处布局断言失败，定位为曲目设置测试残留 Tk 缩放；在清理阶段恢复缩放后全组通过。
- 源码使用独立数据目录通过冒烟；实际点击侧栏新建按钮，检查 1180×800 默认窗口及 960×720 最小窗口，谱面、文本与底部按钮完整可见，最大化／还原和保存后重新编辑通过。[新建工作台截图](docs/new-score-workbench.png) 已检查，独立验证结果位于 `work/new-score/ui-result.json`。
- 成品 `dist/三角洲口风琴_新建简谱版.exe` 为 22,411,496 字节，SHA-256：`358a2b4eec5c3e55f0efbcd847f110eb4f5f88d57206d41f85de596a52bcd981`。管理员清单检查通过，实际 EXE 在独立目录完成包含新建工作台的冒烟，退出码 0；仅冒烟子进程使用 `RunAsInvoker`。报告位于 `work/new-score/package-verification.json`。
- 本轮未向实际游戏发键；游戏内听感未验证。官网公开客户端下载包未更新，使用本地新版需先从系统托盘退出旧版。

## 2026-09-13：修复线上简谱下载并验证实际成品

- 用户在「全源搜索版」下载官网曲谱 JSON 时遇到 `MThd not found. Probably not a MIDI file`。旧 EXE 的下载器仅支持 MIDI；将工作区已有的曲谱格式识别、JSON 校验和原谱保留能力纳入新成品。手动导入、官网曲库与聚合搜索均保留原谱，MIDI 下载继续兼容。
- 官网《小金不臭不臭》的简介已改为「口风琴演奏简谱，支持试听与编辑。」；备份并原子更新目录后完成公开 HTTPS 回读，其余 5 首曲目及该曲谱文件未改变。
- `python -m unittest -q test_online_library test_image_score_download test_resource_search test_resource_search_ui test_music test_transport test_jianpu_rules test_source_editor`：162 项通过。新增官网下载对话框回归使用实际下载处理函数，验证 JSON 载入本地曲库、音符一致、原谱编辑及无自动发键；现有坏文件清理、大小／哈希校验、格式透传与下载去重用例通过。
- 新增 `--smoke --smoke-online "官网曲名"` 可复现真实目录读取、界面选择、下载、载入和编辑检查，仅冒烟模式可用；构建前测试增加 JSON 下载与聚合搜索下载回归。
- 源码与交付 EXE 均使用独立数据目录，从官网实际下载《小金不臭不臭》，通过 SHA-256 校验并保留 44 个音符、10 秒总时长、开头 2 秒休止及 F 调原谱。原谱编辑、窗口最大化与还原、托盘隐藏恢复和搜索入口检查通过。[成品下载成功截图](docs/online-score-download.png) 已检查，简介与成功状态正常显示。
- 成品：`dist/三角洲口风琴_曲谱下载修复版.exe`，22,413,698 字节；SHA-256：`ccc9e246a3f7f03f58ec8f0901b3dc099bf9d9d5a6d6d6a4dc048b0609d85c59`。保留 `requireAdministrator` 清单，隔离成品冒烟仅对子进程使用 `RunAsInvoker`。结果位于 `work/online-score-fix/package-verification.json`。
- 官网公开客户端下载包尚未更新；用户需从托盘退出旧版后运行本地修复版。本轮未向游戏发键，游戏内听感仍需实机验证。`AGENTS.md`、个人曲谱、插件配置和构建产物继续仅保留本地。

## 2026-09-13：私人会话图片识谱插件与曲谱 JSON 官网下载

- 私人插件 `melodica-score` 使用会话视觉转录图片，经现有简谱解析器生成整数毫秒音符和可编辑原谱；仅输出曲谱 JSON，不生成 MIDI。脚本保存在用户个人插件目录，不提交或推送 Git。
- 桌面支持手动导入及从官网目录／聚合搜索下载曲谱 JSON；保留原谱，校验大小、SHA-256、音符及编辑原谱一致性。原有 MIDI 下载回归通过。官方静态目录可使用 `format: "score"` 条目；服务器仍只需提供 JSON 静态文件。
- `python -m unittest -v test_online_library test_image_score_download test_resource_search test_resource_search_ui`：50 项通过；`python -m unittest -q test_music test_transport test_jianpu_rules test_source_editor`：111 项通过；插件内 `scripts/test_score_plugin.py`：10 项通过。
- 插件测试覆盖反复、延音、八度、三连音、尾部休止、不确定识别拒绝生成、游客／账号隔离、重复导入、原谱不一致拒绝导入／发布、真实 App 编辑器、仅本机试听、发布前备份、失败保留旧目录、持锁互斥及重复发布去重。发布测试使用临时目录模拟 SFTP，没有向官网上传测试曲目。
- 主程序隔离冒烟 `python app.py --smoke --data-dir work/image-score-plugin-smoke` 通过；真实窗口和编辑器截图在 `work/image-score-plugin/main.png`、`editor.png`，已人工检查。`work/image-score-plugin/check_ui.py` 完成本机 MIDI 合成器短音试听，正常结束并关闭输出；该合成器仅用于发声，没有创建 MIDI 文件。
- 插件和技能结构校验通过。服务器只读连接测试被现有 SSH 身份拒绝，实际官网上传尚未验证，需有效服务器凭据；没有变更线上曲库或公开下载包。尚无用户实际简谱图片，本轮不声称识别准确率或游戏内效果已经验证。

## 2026-09-13：搜索来源默认全部勾选

- 打开「搜简谱 / MIDI」时，简谱空间、官网曲库、BitMidi、MidiWorld、MidiShow 五个来源默认全部勾选；可手动取消，重新打开窗口时恢复全选。同步更新窗口提示、README、既有默认值回归和成品冒烟断言。
- `python -m unittest -q test_resource_search_ui test_music`：80 项通过。源码与新 EXE 在隔离目录执行 `--smoke` 均通过：五个来源全选、42 个示例音符、编辑器最大化／还原、搜索、托盘及隐藏恢复正常；截图检查见[默认全选窗口](docs/all-sources-default.png)。此轮未发起网络搜索或游戏发键。
- 成品：`dist/三角洲口风琴_全源搜索版.exe`，22,403,643 字节，SHA-256 `a152526df03545ce5516b2daa11bad0374d8798fa83bd79ad4564d6ffb9e4bc1`。冒烟子进程使用 `RunAsInvoker`，未修改官网公开下载包。

## 2026-09-13：MidiShow 人机验证与搜索失败提示

- 复现用户查询「父亲」：MidiShow 返回 HTTP 403，响应头 `server=cloudflare`、`cf-mitigated=challenge`；公开首页和常规浏览器请求头也返回验证页。问题来自站点访问验证，不是解析器把已有曲目漏掉。
- 区分浏览器验证／403 拒绝访问、429 限流及其他 HTTP 失败；即使验证页返回 HTTP 200 也不作为普通搜索结果解析。正常结果页附带 Cloudflare 脚本仍可读取。网络线程用普通错误数据传递访问状态，保留单站失败隔离与取消机制。
- MidiShow 验证时显示「去 MidiShow 搜索」，打开触发本次验证的曲名，不受已选其他来源曲目或尚未提交的新输入影响。无结果时「打开源网页」优先打开失败的来源；来源未成功返回时不再断言无匹配歌曲。分页被拦截时保留已有曲目并停止该站继续翻页，新搜索清除旧验证状态。
- `python -m unittest -q test_resource_search test_resource_search_ui test_jianpu_search test_online_library test_music test_transport test_lifecycle`：143 项通过，新增 9 项覆盖验证页识别、普通结果兼容、状态与入口、失败／空结果区别、新查询隔离、其他来源可用、下一页验证和最小窗口布局。
- 实际联网搜索「父亲」时其他四个来源各返回 0 首，MidiShow 返回验证状态；新窗口显示该站尚未获取结果、网页搜索按钮可见。检查[真实截图](docs/midishow-verification.png)，独立曲库报告保存在 `work/midishow-fix/ui-report.json`。
- 源码与最终 EXE 的隔离冒烟均通过：退出码 0、42 个示例音符，编辑器最大化／还原、简谱规则选择、聚合搜索、托盘及隐藏恢复正常。成品为 `dist/三角洲口风琴_搜索修复版.exe`，22,403,450 字节，SHA-256 `e7f488424e286f6f927a587ada6a2f30b2d47ecd8679e69f58714d98dcdbd74e`；冒烟子进程使用 `RunAsInvoker`，未改动公开下载包。
- 本次修复的是受限状态识别和浏览器衔接，未恢复受 Cloudflare 拦截时的后台自动搜索，也未自动解决验证码、读取浏览器 Cookie 或代替用户登录／积分下载。网页完成验证后可手动下载并导入；未向游戏发键。

## 2026-09-13：谱面跟随版成品（第二阶段）

- PyInstaller 6.11.1 生成 `dist/三角洲口风琴_谱面跟随版.exe`，22,400,810 字节，SHA-256：`9ff22a068e3ce7b7b8d3a51ba0e581fd0045ebe57b6af281dffe5fc37f380539`；PE 清单确认 `requireAdministrator`、`uiAccess=false`。
- 源码和实际 EXE 均在独立目录通过冒烟，退出码 0：重新解析两份曲谱，旧《青花瓷》原谱与 527 个保存音符保持一致；实际打开编辑器，验证最大化、还原以及末尾音符标记在窗口尺寸变化后保留。搜索、托盘、主窗口隐藏／恢复正常。成品报告为 `work/score-follow/verification.json`。
- 首次源码冒烟因自编样例缺少既有冒烟流程要求的 `editor` 字段失败；补齐隔离样例后源码与 EXE 均通过。毫秒定位舍入与保存格式统一后，`test_source_editor` 的 19 项再次通过。
- 桌面代码和验证文档分阶段推送至 `codex/jianpu-search`；新 EXE 本地交付，未修改官网公开下载。原曲库兼容，未执行游戏内发键验证。

## 2026-09-13：编辑器最大化与谱面试听跟随（第一阶段）

- 编辑器改用带原生最大化／还原按钮的窗口边框，保留模态抓取；原谱与精确音符编辑器均支持。窗口尺寸改变时重排谱面并保留正在播放的标记。
- 直接读取播放器实际进度，每 50 毫秒在 Tk 主线程更新绿色谱面标记和谱文位置。定位沿用解析器的源文时间轴，包含反复的每次出现、休止、延音及变速；选段时间轴裁切后从零开始。选区和编辑光标保留，停止、完成、输出异常或编辑后清除标记，过滤旧试听批次的结束回调。未改变音频调度或游戏输入规则。
- 长谱按每页 2000 个符号显示，提供翻页按钮；播放时自动翻到当前符号所在页并滚动到可见位置，反复也能回到前页。替代此前只显示前 2000 个符号的限制；分页仍为编辑预览，跨页连线不属于印刷级制谱。
- `./.venv/Scripts/python.exe -X utf8 -m unittest -q test_source_editor test_jianpu_rules test_jianpu_search test_resource_search test_resource_search_ui test_music test_score_editing test_transport test_lifecycle test_account_client test_accounts`：194 项通过；`test_score_editor` 独立运行 10 项通过，共 204 项。新增覆盖最大化／还原、反复与选段、延音和休止定位、保留选区、停止和异常清理、旧批次过滤、缩放保留标记及跨页回跳。修正了测试中的 Tk 定时器取消对象和异步完成状态等待条件。
- 使用隔离曲库、自编 68 音符练习进行真实 `PreviewOutput` 试听，39.444 秒后「演奏完成」、无错误且标记清除；最大化后还原，谱面持续跟随并滚动。检查[最大化试听截图](docs/score-follow-maximized.png)与[普通窗口跟随截图](docs/score-follow-window.png)，过程记录保存在 `work/score-follow/ui-report.json`。未向游戏发送按键，音色和游戏内效果未作为本次验证结论。

## 2026-09-13：原谱编辑版成品（第二阶段）

- PyInstaller 6.11.1 打包 `dist/三角洲口风琴_原谱编辑版.exe`，22,396,068 字节，SHA-256：`68967223cd00147a701c583d8bc88c20e93dda28fad5a6e13e775b11ffb6f318`；确认管理员清单 `requireAdministrator`、`uiAccess=false`。
- 实际 EXE 在独立目录通过冒烟，退出码 0。重新解析两份曲谱，恢复旧《青花瓷》的原谱编辑和 108 BPM，编辑解析数据与原 527 音符一致，谱面预览非空；搜索、保存入口、托盘、窗口隐藏和恢复正常。成品报告为 `work/source-editor/verification.json`。
- 本地成品交付，代码分阶段推送至 `codex/jianpu-search`。未修改官网或源站曲谱，未执行游戏内发键测试。

## 2026-09-13：原谱文字编辑与实时谱面（第一阶段）

- 根据用户对大量小数时值难以编辑的反馈，参考简谱空间编辑页，加入原谱模式：左侧保留 `/key(...)`、BPM、八度点、减时符号、小节和歌词，右侧使用本地 Tk Canvas 渲染谱面，未执行或嵌入源站脚本。保留原谱的旧导入文件自动恢复；恢复前逐项比较实际保存音符，避免用过期原谱覆盖修改后的音乐。无原谱的 MIDI／云端曲目保留精确格式。
- 点击预览音符定位完整文字，按钮修改八度、升音、时值、附点或插入小节线；修改调号／速度保留后续段落标记，移调只改绝对调号。原谱模式选段从完整解析的时间轴裁切，保留前文转调、速度和反复中的各次出现，延音按选中时段裁切。保存为修改版后再次打开仍保留原谱及解析模式。修改或关闭窗口停止试听，F8／F9 与游戏输入调度未变。
- `./.venv/Scripts/python.exe -X utf8 -m unittest -q test_source_editor test_jianpu_rules test_jianpu_search test_resource_search test_resource_search_ui test_music test_score_editing test_transport test_lifecycle test_account_client test_accounts`：188 项通过；`test_score_editor` 独立运行 10 项通过，共 198 项。谱面布局调整后 `test_source_editor` 的 13 项再次通过。新增回归覆盖全角符号、含表情歌词的位置、旧文件恢复、保存重开、移调、选段上下文、停止释放、原谱不匹配回退及无效编辑清除旧预览。修复测试发现的表情位置偏移和延迟校验回调清理问题。
- 隔离曲库中的真实《青花瓷》恢复为 A3、108 BPM 原谱，仍为 527 音符、228.05555555555338 秒；完整演奏数据与旧导入文件逐项一致。源码冒烟重新解析两份曲谱，并实际打开原谱编辑器，验证显示、音符一致性、搜索窗口、托盘与隐藏／恢复，退出码 0。
- 检查 1180×800 和 960×720 布局，自动布局回归覆盖 Tk 缩放 4/3、2。真实窗口中点击自编样例首音定位到 `1_`，高音按钮将其变为 `1'_` 并显示上方八度点，撤销恢复原音。[编辑器截图](docs/source-score-editor.png)为自编样例；未提交到源站，未向游戏发键。
- 预览上限 2000 符号；超长歌词与延音做紧凑显示，完整音乐仍可编辑保存。当前提供文字编辑和谱面预览，不包含拖拽音符、印刷级制谱或图片识谱。原谱文字和局部连奏附注仍只保存在本地。

## 2026-09-13：简谱规则增强版成品（第二阶段）

- PyInstaller 6.11.1 生成独立文件 `dist/三角洲口风琴_简谱规则增强版.exe`，22,370,336 字节，SHA-256：`0dab386f2ed219d17744c6274ca2e713ff3de3f53f7de8476f52ac6753d8ec09`。PE 清单确认 `requireAdministrator`、`uiAccess=false`。
- 源码和实际 EXE 均在独立数据目录通过冒烟，退出码 0：重新解析并编译 5 份简谱（真实《青花瓷》与两首自编规则样例的两种模式）；逐项比较保存音符和编辑文字，含局部连奏附注。默认谱面模式、规则选择框、托盘、主窗口隐藏／恢复与编辑器正常。成品报告位于 `work/jianpu-rules/verification.json`。
- 使用 Windows 真实 `PreviewOutput` 试听自编反复／延音／连奏样例：8 次起音、3.3333333333333335 秒，调度回调「演奏完成」、无错误，MIDI 输出句柄已关闭。该检查不等同于游戏内按键与听感验证。
- 桌面代码按阶段推送至 `codex/jianpu-search`，未改动官网公开下载。旧程序及曲库文件保留，新规则需使用增强版重新搜索导入；没有执行游戏内发键验证。

## 2026-09-13：补充简谱反复、连线和段落调号（第一阶段）

- 按用户选择，默认「按谱面规则」演奏。加入 `|: :|` 两遍反复、相接／嵌套反复、`[1`／`[2` 一二房子、`~` 同音延音和圆括号连线；多处 `/key(...)` 从标记位置生效。回跳恢复段首速度、调号和歌词转调，歌词位置以原谱音符编号计算，合并延音或展开反复不会导致错位。反复与连线最多嵌套 8 层，展开仍限 30000 音符、30 分钟；不完整结构、跨反复边界连线等报错。
- 源站核对纠正了之前的支持范围判断：2026-09-13 下载的 [je.js](https://jianpu.space/javascripts/je.js)（SHA-256 `a8d76e25ff052833e084430d1652d6bec3b332d50c90c95ab2cd5dd40299859f`）及 [songShow.js](https://jianpu.space/javascripts/songShow.js) 未实现上述反复和圆弧／`~` 连线；最后一个调号用于全曲。用自编片段在限制运行时间、无网络或系统接口的 Node VM 中复核，保留「跟随源站播放」选项，并在本地文件保存规则模式及提示。新增语法是本软件的谱面演奏扩展，不代表源站支持。
- 局部连奏附注经曲谱整理、JSON 保存、编辑器、移调、片段裁切和播放调度保留。延音合并为一个长音；连奏重复音仍重吹，衔接保留至多 25 毫秒间隔。模拟时钟验证完整演奏顺序、释放时刻及持续长音中停止后的释放。云端四整数音符协议保持兼容，局部连奏附注仅保存在桌面；反复展开和合并长音可随标准音符同步。
- `./.venv/Scripts/python.exe -X utf8 -m unittest -q test_jianpu_rules test_jianpu_search test_resource_search test_resource_search_ui test_music test_score_editing test_transport test_lifecycle test_account_client test_accounts`：175 项通过。`test_score_editor` 独立进程 10 项通过。调整编辑器连奏分组后，相关 `test_jianpu_rules test_score_editing test_resource_search_ui` 37 项再次通过。首轮命令误用了不存在的 `test_cloud_score`，已改为仓库现有的账号与交换格式测试。
- 实际联网重新导入《青花瓷》：527 音符、228.05555555555338 秒，与此前源站逐音符基准比较音高和起止时间，误差小于 `1e-8` 秒；旧谱的重复音、节奏与歌词转调结果未改变。两首自编规则样例分别验证两种导入模式及编辑文字还原。
- 检查自建隔离窗口的 960×680、860×630 搜索布局及编辑器，规则选择、导入、试听和保存按钮可见；[规则窗口截图](docs/jianpu-rules.png)使用自编样例，未将样例提交到源站。未向游戏发键，游戏内音色与听感仍待实机试奏。D.C.／D.S.、更多房子或指定次数、连音组和装饰音语法仍未支持，详见 README。

## 2026-09-13：修复《青花瓷》简谱导入失败

- 实际复现：简谱空间的《青花瓷》包含歌词内的 `(+1key)` 标记，原解析器主动拒绝转调谱；间奏音符行还混入普通字母 `ji`。新增回归先确认 3 项失败，再实现歌词音节、引号分组、前奏星号和延续下划线的定位，休止与延音不占歌词音符位置，转调从对应音符起累计生效。普通字母杂字保留行号提示，升降前缀及未知演奏符号仍严格校验。
- `./.venv/Scripts/python.exe -X utf8 -m unittest -q test_jianpu_search test_resource_search test_resource_search_ui test_music test_score_editing test_transport test_lifecycle`：129 项通过。新增 5 项覆盖转调位置、累计升降调、无对应音符／引号不完整／音高越界、杂字不吞音与不改节奏，以及导入后重载、编辑与处理提示保留。
- 实际联网导入 527 个音符、约 228.056 秒，A3 调号、108 BPM。使用谱源公开解析器在无文件和网络接口的独立 Node 上下文中取得参考结果，逐项比较全部音符音高、起止时间和总时长，一致（浮点时间误差小于 1e-8 秒）。升半音位于第 406 个旋律音，约 178.333 秒；处理 1 处转调，并提示第 46 行的 `ji`。原谱与参考数据仅放在忽略的 `work/qinghuaci-fix/`，未提交完整网上曲谱。
- PyInstaller 6.11.1 生成独立成品 `dist/三角洲口风琴_简谱搜索修复版.exe`，22,362,702 字节，SHA-256：`88700d56f430b2c01c1ac8efb91599c590672e79c9d23908a703540d178e7f97`。管理员清单确认 `requireAdministrator` 与 `uiAccess=false`。源码和成品隔离冒烟通过，均重新解析《青花瓷》原谱并与保存的音符逐项比较；编辑文字、默认简谱搜索、托盘及隐藏／恢复正常，退出码 0。
- 成品冒烟仅对子进程设置 `__COMPAT_LAYER=RunAsInvoker`，未更改成品清单、系统设置或个人曲库。本次未改界面布局，未向游戏发送按键，未把音符对照当成游戏内听感验证。修复推送至既有纯桌面 `codex/jianpu-search` 分支，保留本机安卓历史保护，不更新官网公开下载包。

## 2026-09-13：简谱搜索窗口与 Windows 成品（第二阶段）

- 主入口改为「搜简谱 / MIDI」，默认搜索简谱空间；结果可直接导入或导入后本机试听，自动选中曲库里的 `[简谱]`，使用「原谱 · 分音」，可继续编辑。默认速度、调号和和弦处理提示在重新加载后仍可查看；主窗口说明支持换行。
- `./.venv/Scripts/python.exe -X utf8 -m unittest -q test_jianpu_search test_resource_search test_resource_search_ui test_online_library test_music test_transport test_lifecycle test_score_editing test_update`：136 项通过。`test_score_editor` 在独立进程中另有 10 项通过，合计 146 项。新增界面回归覆盖默认简谱来源、导入后编辑与重载、无效谱不替换当前曲目、试听先解除模态，以及下载完成不打断已有演奏。
- 实际联网搜索并导入《小星星》42 音、《茉莉花》67 音、《月亮代表我的心》120 音。第三首的独立和弦行被明确忽略，并保留处理提示；三首均缺少速度和调号标记，使用已提示的默认值。原谱未提交或上传。
- 自建 Tk 窗口、隔离曲库验证完整搜索与导入流程，并检查[真实搜索窗口](docs/jianpu-search.png)、860×630 最小搜索窗口、1040×780 主窗口和简谱编辑器；搜索、导入、试听和演奏入口可见。其他检查截图仅保存在 `work/jianpu-ui/`。使用真实 `PreviewOutput` 试听导入《小星星》的前四音，回调为「演奏完成」、无错误，已释放音符；未向游戏发送按键，不等同于游戏内听感验证。
- PyInstaller 6.11.1 生成 `dist/三角洲口风琴_简谱搜索版.exe`，22,360,633 字节，SHA-256：`cbcd058e718d21ac0fcd44019341a84a2f80817f5dc5711a7099c8fe012a981b`。PE 检查确认 `requireAdministrator`、`uiAccess=false`。源码及成品均通过隔离冒烟：3 首在线简谱可解析、可编译并可读取编辑文字，默认简谱搜索、编辑器、托盘和主窗口隐藏／恢复通过，退出码 0；成品报告位于 `work/jianpu-package-smoke/verification.json`。
- 成品冒烟仅对子进程设置 `__COMPAT_LAYER=RunAsInvoker`，未更改成品清单或系统设置。成品为独立本地预览文件，未覆盖旧版或更新官网。当前开发分支的安卓历史触发本机推送保护后，从既有纯桌面分支建立 `codex/jianpu-search` 并逐阶段移植推送；未移除或绕过保护、未推送安卓内容。

## 2026-09-13：在线简谱搜索与解析（第一阶段）

- 接入简谱空间公开曲目目录，按曲名和歌手搜索，支持繁简体及全角匹配；只解析站内曲目链接。原有四个 MIDI 来源继续可用。
- 文字简谱读取音高、八度、升降音、附点、延音、休止和分段速度；明确忽略独立和弦标记，避免将 Em7／G7 中的数字误读成旋律。未标速度按 120 BPM、未标调号按 1=C4，并保留提示。不支持的重复、连线、转调或其他符号明确报错，不静默跳过。
- `./.venv/Scripts/python.exe -X utf8 -m unittest -q test_jianpu_search test_resource_search test_music test_score_editing`：89 项通过，新增 14 项覆盖节奏、变速、调号、默认值、和弦、超限、未知记谱、繁简匹配、错误页面、编辑往返、去重、取消与写入失败清理。系统 Python 缺少 Mido，完整验证使用项目虚拟环境。
- 联网读取并导入《小星星》（42 音、24 秒）、《茉莉花》（67 音、28 秒），确认数字谱直接进入演奏计划，源文和默认值提示保留。测试谱仅保存在忽略的 `work/jianpu-live/`，未提交网上曲谱，未向游戏发送按键。界面与成品验证记录见后续阶段。

## 2026-09-13：撤回 MP3 自动扒谱试验

- 按用户要求移除扒谱入口、音频处理进程、旋律识别算法、专用构建脚本和依赖、相关测试及说明。MIDI 聚合搜索、简谱编辑和播放代码恢复至加入扒谱前的版本。
- `python -m unittest -q test_resource_search test_resource_search_ui test_music test_transport test_lifecycle`：100 项通过。独立曲库检查主窗口没有扒谱按钮或对话框属性，并完成[真实窗口截图](docs/audio-feature-removed.png)检查。
- 重新打包 `dist/三角洲口风琴_聚合搜索版.exe`，在没有相邻音频引擎的情况下执行隔离 `--smoke`：退出码 0、`ok=true`、42 个示例音符，聚合搜索、编辑器、托盘及隐藏恢复通过。成品 SHA-256：`890b4d48d1f40b65ec782e5376a00cd2251311c8294efbe105ee8e991f5d4240`。
- 旧音频扒谱 EXE、完整压缩包及引擎移至忽略的 `releases/音频扒谱试验_已停用/`，停止该试验的优化。原始 MP3 和既有曲库文件未修改；本轮未向游戏发键。

## 2026-09-13：聚合搜索与下载转换预览（Windows）

- 新增官网曲库、BitMidi、MidiWorld、MidiShow 四个来源，后台并行搜索、逐站返回、取消与新请求隔离、加载更多、来源标记、源网页入口，以及下载校验、自动旋律与八键转换、按内容去重、本机试听。MidiShow 仅搜索与打开源站，登录／积分下载完成后通过现有 MIDI 导入接入。
- `python -m unittest test_resource_search test_resource_search_ui test_online_library test_music test_transport test_lifecycle test_song_settings test_score_editing test_update`：**133 项通过**。新增 19 项覆盖链接解析、中文编码、来源分页、站点失败隔离、验证码页识别、下载失败清理、取消清理、重复内容去重、晚到结果过滤、转换后曲库选中、试听前解除模态、关闭与切换账号隔离、最小窗口布局。
- `test_score_editor`（10 项）与 `test_account_client`（16 项）分别在独立 Python 进程中通过；共验证 159 项。首次将全部模块放入单进程运行出现 3 处布局断言失败：编辑器 2 处、账号 1 处；已有 `test_song_settings` 会调整 Tk 缩放而不恢复，单独复验均通过。本轮未修改既有缩放测试，也未将第一次合并运行计为全部通过。
- 实际联网验证：`river` 在 BitMidi 返回 15 首、MidiWorld 返回 30 首、MidiShow 返回 20 首；中文 `稻香` 的 MidiShow 页面解析到 20 首。BitMidi 与 MidiShow 确认有下一页；通过真实页面链接解析下载地址，未使用猜测的文件编号或登录态接口。
- 实际下载并转换：MidiWorld 的 River of Dreams (Billy Joel) 生成 476 个演奏音符；BitMidi 的 A.HAMMOND.Down by the river 生成 570 个演奏音符。测试 MIDI 仅保存在忽略的 `work/resource-live/songs`，未提交资源文件。使用其中前 4 个音符调用真实 `PreviewOutput`，MIDI 合成器正常完成并释放，回调为“演奏完成”、无错误；不等同于游戏内听感验证。
- 通过 computer-use 检查最终真实[聚合搜索窗口](docs/aggregate-search.png)，65 条结果可滚动浏览，来源、条件、精选入口及下载按钮可见。自动化检查 960×680 与最小 860×630 窗口，包含四个来源同时失败时的布局。
- 源码及最终 EXE 的隔离 `--smoke` 均为退出码 0、`ok=true`、42 个示例音符、`aggregate_search=true`；EXE 另读取并编译 2 个真实下载 MIDI。托盘、隐藏恢复、编辑器仍正常。首次新增成品检查仅处理空闲事件，窗口尚未映射导致失败；改为处理完整 Tk 事件后源码和重建成品通过。
- 成品：`dist/三角洲口风琴_聚合搜索版.exe`，22,350,800 字节；SHA-256：`4890ce446dba4887ab0d3cc62c9a4067af6eae8e0b4c76027ec48b3ccd3a8609`。PyInstaller 使用既有单文件与管理员参数；隔离冒烟子进程使用 `RunAsInvoker`。本次未更换官网公开下载包。
- 第一次在含手机端历史的本地分支推送被原保护钩子拦截，已在纯桌面 `codex/aggregate-midi-search` 分支分阶段提交推送。保护钩子保留，测试数据、MIDI 和构建产物均不上传。
- 限制：网站可能限流、出现验证或变更页面；BitMidi 个别详情页曾返回 HTTP 403，程序会显示错误并保留其他来源结果。普通 MP3／视频扒谱和图片简谱识别不在此版本内。未使用 `--game-test`，游戏内八键响应、音色和演奏效果仍需实机验证。

## 2026-09-13：统一乐曲编辑器预览版（Windows 本地交付）

- 新增「编辑选中乐曲」，MIDI、旧简谱、内置示例及云端曲谱共用文本简谱编辑器。支持 BPM、整曲移调、撤销／重做、选段试听和另存修改版，保留连奏／分音；曲库增加来源类型标记。MIDI 按当前音轨与演奏方式提取完整原速旋律，原文件不改写。
- `test_score_editing` 新增 5 项，覆盖跨速度表 MIDI 转换、重复音与不足一毫秒的短音、前后休止、所选钢琴旋律及伴奏尾音、完整 MIDI 音域、无损文字移调和非法输入。精确拍数在转换／解析间按 8 位小数秒对照，持久化沿用既有整数毫秒格式。
- `test_score_editor` 新增 10 项，覆盖原曲保留、另存选中和重新编辑、云同步读取兼容、所选 MIDI 音轨与原速全曲范围、一次撤销恢复整曲移调、无效输入与磁盘失败、未保存退出提示、选段试听、F9 回调及退出后的资源释放、元数据不一致回退。100%／150% Tk 字体缩放与 820×650／920×710 窗口检查保存按钮和编辑区可见。
- 按独立 Python 进程依次运行 `test_score_editing`（5）、`test_score_editor`（10）、`test_music`（59）、`test_transport`（17）、`test_song_settings`（16）、`test_account_client`（16）、`test_update`（4）、`test_online_library`（8）、`test_lifecycle`（5），共 **140 项全部通过**。初次合并运行出现 4 处失败：进度拖动未收到预期事件、账号和主窗口受跨用例 Tk 缩放影响；新增编辑器测试已恢复原缩放值，按独立进程复验全部通过。未将首次失败计为通过。
- 源码与实际 EXE 均使用隔离目录运行 `--smoke`，均为退出码 0、`ok=true`、42 个示例音符、1180×840 主窗口，托盘及隐藏／恢复正常；冒烟新增编辑器导入、打开、解析、保存按钮可见及关闭检查，结果 `editor=true`。新增按钮检查首次仅处理空闲事件，窗口尚未映射导致失败；改为处理完整 Tk 事件后源码与成品均通过。
- 使用 computer-use 检查真实[曲库窗口](docs/score-editor-library.png)及[编辑窗口](docs/score-editor-window.png)，按钮与文字完整显示；实际点击编辑器本机试听和停止按钮，状态正常切换。本轮未向游戏发送演奏按键，游戏内听感尚待试用。
- 成品：`dist/三角洲口风琴_乐曲编辑预览版.exe`，22,315,443 字节；SHA-256：`bba1c592b0216bc6d946a3e17c6a75e7cdc3456e9d2fc880ead58a39b4e3e6e2`。使用 PyInstaller 6.11.1 与原有单文件管理员参数，PE 清单 `requireAdministrator` 与 `uiAccess=false` 验证通过；保留既有 `pkg_resources` 隐式导入警告。仅隔离冒烟子进程使用 `RunAsInvoker`，成品清单及系统设置不变。
- 本地分支历史包含手机端内容，首次推送被已有保护钩子拦截；已从纯桌面远程分支创建独立工作树，将桌面修改按阶段移植并推送至 `codex/score-editor-desktop`。保护钩子保留；未修改官网公开下载包。

## 2026-09-13：账号界面优化版 Windows 打包（仅本地交付）

- 使用 PyInstaller 6.11.1 和既有 `--onefile --windowed --uac-admin --hidden-import mido --hidden-import pystray._win32` 参数构建 `dist/三角洲口风琴_账号界面优化版.exe`。成品 22,299,162 字节，SHA-256：`ea410102f5d6bf4d8f4a8f8a4c71a08afc7bb30671f5ca79a36ed5b9314612ab`；管理员清单与 `uiAccess=false` 检查通过，保留既有 `pkg_resources` 隐式导入缺失警告。
- 实际成品使用独立 `work/account-form-cleanup/package-smoke` 目录运行 `--smoke`，退出码 0、`ok=true`，42 个示例音符、1180×840 主窗口、托盘与隐藏／恢复通过。仅冒烟子进程指定 `RunAsInvoker`，未改变成品清单或系统设置，未注册演奏热键、读取个人曲库或连接线上账号服务。
- 最终源码账号回归复测 16 项通过；三个账号页面的视觉检查见下节截图。成品不覆盖旧版本，源码及记录按阶段推送至 `codex/interface-refresh-preview`，未更新官网服务或公开下载包。本轮未验证游戏内演奏或真实邮件投递。

## 2026-09-13：精简 Windows 登录、注册与找回密码表单

- 旧账号面板将三个流程的字段全部放在一起，登录时仅禁用发送按钮。现在登录只显示邮箱、密码及登录按钮，整组验证码控件隐藏且不能发送；注册与找回密码分别展示设置密码／新密码和必要的邮箱验证，按钮简化为「注册」「重置密码」，说明按流程切换。
- 切换表单保留邮箱并清空密码、验证码及旧提示；重置失败保留当前表单便于重试，成功后返回密码登录。登录窗口缩短至 540×500，注册、找回与已登录账号面板保持 540×650；注册继承与同步、退出后的游客曲库以及后端认证规则沿用原行为。
- 新增回归先在旧界面复现登录页错误显示 3 个输入框；修复后 `python -X utf8 -m unittest -v test_account_client test_accounts` 共 28 项通过，覆盖控件显隐、重复切换、清除旧凭据、密码登录失败后重试、重置失败后重试并使用新密码登录及既有账号隔离。重置测试首次因刚发送注册邮件触发 60 秒冷却失败，测试改为推进模拟时钟后通过，服务端限流不变。保留既有 Starlette 弃用警告。
- `python -X utf8 -m unittest -q test_music` 59 项通过。源码 `app.py --smoke --data-dir work/account-form-cleanup/source-smoke` 通过，42 个示例音符、1180×840 主窗口、托盘和隐藏／恢复正常。
- 使用隔离目录和原生窗口截图检查[登录](docs/account-form-login.png)、[注册](docs/account-form-register.png)、[找回密码](docs/account-form-reset.png)，字段、主按钮与返回入口显示完整。本轮账号联调使用临时数据库与假邮件，未发送真实邮件或更改个人账号，未向游戏发键。

## 2026-09-13：游客曲库修复版 Windows 打包（仅本地交付）

- 使用 PyInstaller 6.11.1 按既有 `--onefile --windowed --uac-admin --hidden-import mido --hidden-import pystray._win32` 构建 `dist/三角洲口风琴_游客曲库修复版.exe`。成品 22,298,316 字节，SHA-256：`2a32d40ce74adc4689bbf5775f99ff5bd07c6824c23fdffa3b5d9edcc4ecb35d`；PE 资源确认管理员权限清单与 `uiAccess=false` 有效。构建保留既有 `pkg_resources` 隐式导入缺失警告。
- 仅复制本机 `songs/`、归属记录和每曲设置到 `work/guest-library-fix/copied-library-smoke`，不复制会话、不修改个人原数据、不调用线上账号服务。源码曲库列表与副本中 21 首本地曲目逐项一致；运行成品 `--smoke` 成功解析全部 16 首 MIDI，退出码 0，42 个示例音符、1180×840 主窗口、托盘及隐藏／恢复通过。
- 冒烟子进程单独使用 `__COMPAT_LAYER=RunAsInvoker`，未改变成品清单或系统设置，未注册演奏热键或向游戏发键。验证脚本与聚合结果仅保存在本机 `work/guest-library-fix/`。
- 修复成品另存于忽略的 `dist/`，源码和验证记录按阶段提交至既有桌面预览分支；未更新官网公开下载包。

## 2026-09-13：修复退出登录后游客曲库被隐藏

- 原因：账号继承将原游客文件名写入 `guest-claims.json`，曲库列表又用同一份归属记录过滤游客文件，导致退出登录后原曲不可见，旧版仍能显示。现在游客直接读取旧 `songs/` 目录，归属记录仅用于防止重复继承，不再控制本地显示；账号专属目录仍独立。
- 回归先复现两处失败：后台注册并退出后游客列表为空，以及加载旧归属记录重启后 MIDI／简谱缺失。修复后验证恢复显示、曲谱可解析、原文件字节保留、速度／移调记忆，以及另一账号不会继承同一批曲谱。
- `python -X utf8 -m unittest test_accounts test_music test_transport test_lifecycle test_online_library test_update test_account_client test_song_settings`：133 项通过。首次将 `test_song_settings` 放在播放／布局测试前时，已有多缩放测试留下的 Tk 全局缩放引起 3 处断言失败；播放／生命周期独立进程复测 22 项通过，按上述顺序完整复测通过。保留既有 Starlette 弃用警告。
- 源码冒烟 `app.py --smoke --data-dir work/guest-library-fix/source-smoke` 通过：42 个示例音符、1180×840 主窗口、托盘及隐藏／恢复正常。使用隔离测试数据检查[游客曲库恢复截图](docs/guest-library-restored.png)与[账号面板说明](docs/guest-library-account-note.png)，已归属曲目可见，说明和底部按钮完整显示。
- 本轮未向游戏发键，游戏内演奏未作为本轮验证结论；个人原始数据保持原位，无需清理归属记录或重新导入。

## 2026-09-13：界面预览版 Windows 打包（仅本地交付）

- 按用户要求构建 `dist/三角洲口风琴_界面预览版.exe`，独立命名，不覆盖原正式版。使用 Python 3.13.5、PyInstaller 6.11.1、Mido 1.3.3、Pillow 12.3.0，以及 `--onefile --windowed --uac-admin --hidden-import mido --hidden-import pystray._win32`；构建前运行 `python -m unittest test_music`，59 项通过。
- 成品共 22,298,479 字节，SHA-256：`d456b483a69c6cdcf5f36427fc5d69067dbe2e09791b2b63a8bfc10fcf03bb93`。PE 资源确认 `requireAdministrator` 与 `uiAccess=false` 保持有效。
- 实际运行成品 `--smoke --data-dir work/ui-package-smoke`，仅该冒烟子进程通过 `__COMPAT_LAYER=RunAsInvoker` 使用普通权限，不修改系统设置或成品清单。退出码 0，42 个示例音符、1180×840 主窗口、托盘就绪、隐藏与恢复均通过；未读取个人 MIDI，未注册演奏热键或向游戏发键。
- 构建日志仍包含既有 `pkg_resources` 隐式导入缺失警告；打包和成品启动均成功。本轮未单独验证管理员实机游戏演奏，先前旧程序占用 F6 的限制仍需试用时留意。
- EXE 仅留在忽略的本地 `dist/` 目录；只提交与推送构建记录，未部署网站、服务或更新线上下载包。

## 2026-09-13：网站主页视觉优化（未部署）

- 重做导航、首屏、客户端展示、功能、教程、键位、注意事项和下载区；主页与账号页采用统一品牌标识和配色。保留公开下载 URL、统计调用、视频脚本、清单和全部风险说明，未修改正式版本号。
- `node test_homepage.cjs`：本机 Chrome 检查通过，覆盖 320 / 390 / 768 / 1440 px 无横向溢出、操作入口不裁切、图片解码、下载地址、锚点目标、跳转主内容的键盘焦点、点击前不加载媒体以及网络错误后的重试／MP4 入口。截图前实际滚动并等待懒加载图片，补齐图片宽高以保留布局空间。
- `node test_account_web.cjs`：官网账号端到端联调通过，使用本机临时数据库与假邮件，验证注册、游客继承、同步、账号隔离与密码重置；未发送真实邮件，也未调用线上账号服务。
- 桌面与手机整页、功能卡片、游戏设置说明截图完成视觉检查。存档见 [桌面主页](docs/homepage-refresh-desktop.png)、[手机主页](docs/homepage-refresh-mobile.png)。视频实际音画与游戏演奏未作为本轮实机结论。
- 初次推送被本机保护钩子拦截，原因是开发基线含仅限本地的安卓历史。保留钩子，从已推送的 `origin/codex/accounts-cloud` 创建纯桌面分支 `codex/interface-refresh-preview`，仅摘取本轮桌面与网页提交后正常推送；未推送手机内容，未部署任何网站、API 或发行包。

## 2026-09-13：客户端界面优化（未发布）

- 更新 Windows 主界面的配色、标识、按钮层级、曲库滚动条、参数布局和八键显示；输入后端、播放调度、数据目录及版本清单没有改动。
- `python -m unittest test_music test_transport test_lifecycle test_song_settings test_online_library test_update test_account_client`：原有 119 项通过。新增最小窗口回归后单独运行 `test_lifecycle`，5 项通过；覆盖默认 1180×840 和最小 1040×780，旋律画布至少保留 40 像素，参数、播放、停止和底部提示完整可见。
- `python app.py --smoke --data-dir work/ui-refresh-final-smoke`：通过，42 个示例音符、托盘就绪、隐藏与恢复通过。使用独立数据目录，未读取个人 MIDI。
- 使用原生窗口截图检查默认尺寸，发现旋律区过薄后合并参数行并调整最小高度；最终截图见 [客户端预览](website/assets/main-window-refresh.png)。源码预览正常，本轮未打包新的发行 EXE，也未进行游戏内发键测试。
- 补充运行 `python -m unittest test_overlay`：9 项中 8 项通过；`test_07_close_main_keeps_real_f6_hotkey_and_f7_panel` 在注册 F6 的前置断言失败，未进入实际 F6 发键步骤。当时系统中仍有 `三角洲口风琴_账号预览版` 进程及其悬浮窗，未为测试结束用户程序；此项未计为通过，需退出占用热键的旧程序后再验证。其余悬浮窗穿透、焦点返回、显隐与播放保持检查通过，输入目标均为自建窗口。
- 代码保存在独立预览分支，未部署网站、服务或客户端发行包。

## 2026-09-13：账号服务与阿里云邮件上线

- 独立账号 API 与官网账号页已部署至现有阿里云服务器，入口为 `https://aiygzn.top/melodica/account/`。主页增加「账号 / 我的曲库」入口，公开下载继续使用 v0.13。
- 阿里云杭州邮件推送已开通，发件地址为 `no-reply@mail.aiygzn.top`。发信子域名 SPF、2048 位 DKIM、DMARC、MX 均已验证；`smtpdm.aliyun.com:465` 的 TLS 及 SMTP 认证通过。密码通过本机遮蔽输入传入服务器，环境文件仅 root 可读，未提交至 Git。
- 服务器使用独立 Python 3.13.15 与固定哈希依赖，生产环境运行 `test_accounts` 共 12 项通过，随后移除临时测试依赖。SQLite 数据及验证码密钥权限为 0600，数据目录为 0700。
- `delta-melodica-accounts`、Nginx、原统计服务均运行正常，账号服务仅监听 `127.0.0.1:3003`。公网 HTTPS 健康检查 200；账号页面及 JS/CSS 返回 200；游客访问私有曲库返回 401。浏览器实测游客点击同步会提示登录，页面显示正常。
- Nginx 配置检查通过；原站点版本清单、公开曲库及统计服务代码哈希保持一致。首页及 Nginx 部署前备份位于 `/var/backups/delta-melodica/accounts.XIXVd4mr`。
- 用户在本机验证窗口输入 QQ 邮箱实际收到的注册和找回密码验证码，线上 HTTPS 流程共 11 项全部通过：真实邮箱注册、游客归属重复请求幂等、原生令牌与网页 Cookie 会话曲库互通、私有曲谱上传下载、Cookie 的 HttpOnly/Secure/SameSite 属性、测试曲谱清理、真实邮件找回密码、重置后旧原生及网页会话失效、旧密码登录失败、新密码登录成功、退出撤销会话。
- 验证账号保留供用户使用，临时验证曲谱已清理，用户设置的最终账号密码未写入本地报告或 Git。上述互通结论来自真实接口会话；邮件收件箱分类、客户端实机互通及游戏内演奏未在本次验证中单独确认。

## 2026-09-13：账号与云端曲库第二阶段

- 新增 `test_accounts` 与 `test_account_client` 共 24 项通过：邮箱验证、登录、找回密码、会话失效、私有曲库隔离、DPAPI、游客继承、响应丢失／复制失败后恢复、重复同步去重、另一设备下载、Tk 后台注册及自动同步。
- `test_music test_transport test_song_settings test_online_library test_update` 共 103 项通过；`test_lifecycle test_overlay` 共 13 项通过。保留暂停、停止释放、悬浮窗及托盘行为。
- 源码 `app.py --smoke --data-dir work/account-smoke` 通过：1120×850、42 音符、托盘、隐藏恢复。注册面板与主窗口截图已检查，底部按钮可见；截图见 `docs/accounts-windows.png`。
- `test_account_web.cjs` 在隔离 Chrome 与本机假邮件服务上通过：游客登录提示、浏览器游客继承、账号隔离、另一浏览器同步、找回密码、旧会话失效、HttpOnly Cookie、无明文浏览器凭据及 390px 手机布局无横向溢出。截图见 `docs/accounts-website.png`。
- Windows 账号预览 EXE 已由 PyInstaller 打包成功；EXE 未执行游戏内发键测试，界面冒烟结论来自源码运行。
- 同步为手动合并；不传播删除和设备设置。此阶段完成时公开账号服务及 SMTP 尚未部署，后续上线进度见本文件顶部；不将假邮件测试视为投递成功。

## 2026-09-13：精简视频播放按钮

- 按用户要求移除播放按钮内的「按需加载 · 流媒体播放」副标题及其专用样式，按钮仅保留「播放功能介绍」，更新样式缓存标识。HLS 播放器、分片、说明区和其他服务不变；旧页面与样式备份在服务器 `/var/backups/delta-melodica/button.Vvjj6H5J`，本次无需 reload 或重启服务。
- 公网页面与本地字节一致，[桌面截图](docs/website-video-desktop.png)确认副标题消失、按钮居中正常。静态文件、媒体 MIME、MP4 Range、404、私有统计 401 和版本曲库检查通过。首轮浏览器检查完成播放及 103 秒定位后，在故障分支页面加载时超时；再次完整回归在定位等待时超时，未将完整回归计为通过。未因这些等待超时改动播放逻辑。

## 2026-09-13：官网功能介绍视频与 HLS 流媒体

- 上传用户提供的 2 分 21 秒功能介绍视频，首页增加导航、观看入口、实录封面和播放器，录制版本 v0.12 与当前软件下载 v0.13 分别说明。保留原始素材，网页副本保持 1920×1080、60 帧与原 AAC 音轨，大小从 244,584,936 字节降至 93,004,767 字节；MP4 SHA-256：`64d938d0027c77ab6a5fcd27b3a185bd19f73aff7c9585cfecdc07936499bb5e`。
- 按用户追加要求使用 HLS 点播：36 个约 4 秒的 TS 片段和完整 VOD 清单，支持拖动定位。移除封装中先于 SPS 的缓冲 SEI 后，全片视频／音频解码检查无报错。流媒体清单 SHA-256：`2e338af5c0dfe0c5ac0743ef7c832270089352543bc5a436cc515a0eb6859a8a`；36 个服务器分片与本地哈希逐一一致。
- 原生 HLS 优先，其他支持 MSE 的浏览器按需加载本站固定版本 hls.js 1.7.2，不依赖访客访问 CDN；保留上游版权声明与 Apache-2.0 许可。默认无视频源、`preload=none` 且不自动播放；加载失败显示可重试入口及 MP4 兼容播放链接。单码率 HLS，不宣称自适应多清晰度切换。
- 隔离 Chrome 中分别验证原生 HLS 与强制走 hls.js 的兼容路径，本地和公网均通过：首屏媒体请求 0，点击后只请求少量片段而不是整个 MP4，真实解码 1080p 画面并播放推进；拖动至 103 秒后请求对应分片并继续超过 104 秒；模拟清单 404 时可重试、兼容链接可用；无页面脚本异常。测试拦截了 `api/view`，未增加生产浏览统计。命令及脚本在本机 `work/intro-video/verify-player.cjs`。
- 390／320 像素窄屏均无横向溢出，导航链接完整；检查[桌面截图](docs/website-video-desktop.png)和[手机宽度截图](docs/website-video-mobile.png)，播放器比例、文字和兼容入口正常。此项为浏览器响应式验证，未将其作为 iOS／Android 真机测试结论。
- 公网核对首页、样式、播放器、封面及清单与本地字节一致；HLS 清单为 `application/vnd.apple.mpegurl`、TS 为 `video/mp2t`，MP4 首尾 Range 均返回 206 且内容一致，缺失清单／片段返回 404。官网版本仍为 v0.13、线上曲库仍为 5 首、健康接口正常、私有统计页和接口匿名访问仍为 401。
- 先上传媒体与播放器，再发布首页；Nginx 配置检查通过后仅执行平滑 reload，主进程 PID 不变，下载统计服务未重启，其他容器状态保持原样。旧首页、样式与配置备份在服务器 `/var/backups/delta-melodica/video.zv3rOP0i`。源码、封面、清单与文档提交，MP4 和 TS 大文件不提交 Git；安卓预览版保持本地独立。

## 2026-09-12：Windows v0.13 正式发布

- 版本号、线上曲库请求标识、官网介绍、公开更新清单与下载跳转统一升级到 v0.13；包含夜莺、守望、风起、破晓四首内置解锁乐曲及简谱保存按钮显示修复。安卓预览版不参与本次发布。
- `build.ps1` 中的 `test_music` 59 项通过；`python -m unittest -q test_update test_online_library test_transport test_song_settings test_lifecycle` 48 项通过，共 107 项。
- PyInstaller 6.11.1 成功构建，保留已知的 `pkg_resources` 隐式导入缺失警告。核对成品内嵌版本为 0.13、8 首内置曲谱与源码一致且可生成播放计划。成品使用独立临时目录和仅作用于该子进程的 `RunAsInvoker` 执行冒烟，退出码 0、`ok=true`，42 个示例音符、托盘和隐藏恢复正常；原管理员清单未修改。
- 隔离源码窗口截图 `build/v013-main.png` 检查通过：v0.13、8 首完整曲名、更新入口及底部操作按钮显示正常。本轮未向真实游戏发键，未重新验证提权启动或游戏内听感。
- 正式成品 `dist\三角洲口风琴_v0.13.exe` 为 21,818,573 字节，SHA-256：`bfa995b5eb09cfd9c04b9617ce7a5ab24715b39f438133de63d0d0383ac1ed45`。服务器文件及公网完整下载的大小与哈希完全一致。
- 官网 `https://aiygzn.top/melodica/` 和客户端更新读取均返回 v0.13；旧版 0.12 比较结果为可更新，版本清单保持 `no-store`。新包与下载服务就绪后才原子发布版本清单；v0.12 成品保留可下载，旧页面、清单及服务代码备份在服务器 `/var/backups/delta-melodica/v0.13.XXAyPs0v`。
- 下载处理器在服务器 Python 3.6 中隔离验证返回 302 到新包，不增加真实计数。仅重启 `delta-melodica-stats`，健康检查通过；公网统计页与统计接口匿名请求均为 401。未修改统计数据、认证配置或线上曲库，5 首曲目目录哈希保持不变；Nginx 未重启，其他容器运行状态保持原样，原已关闭的美业运营助手保持停止。

## 2026-09-12：Windows 内置四首解锁乐曲

- 从用户本地简谱读取原始音符及 100 BPM，加入「乐曲解锁-夜莺」「乐曲解锁-守望」「乐曲解锁-风起」「乐曲解锁-破晓」。按用户更正使用「风起」，原有自定义曲库文件保留。
- 使用独立空曲库逐首选曲，核对内置内容与原 JSON 完全一致，分别为 7／10／7／11 音、4.2／6.0／4.2／6.6 秒；均按原谱分音生成播放计划，无音域折回，不自动演奏。8 首内置曲目均成功导出 MIDI 并回读核对音高与时长；MIDI 整微秒速度量化允许 0.1 毫秒时长误差。[主界面截图](docs/unlock-songs.png)检查通过，四个完整曲名均可见。
- `python -m unittest -q test_song_settings`：15 项通过；`build.ps1` 中的 `test_music`：59 项通过，共 74 项。源码 `--smoke --data-dir .\build\smoke-unlock-songs` 通过，主界面、托盘和隐藏恢复正常。
- PyInstaller 6.11.1 构建成功；保留已知的 `pkg_resources` 隐式导入缺失警告。成品内嵌 `music` 模块的 8 首曲谱与源码完全一致，均可解析并生成播放计划。对成品子进程临时设置 `RunAsInvoker`，使用独立临时数据目录执行 `--smoke`，退出码 0、结果 `ok=true`；成品管理员清单未修改，本轮未验证提权启动或游戏内发键。
- 交付 `dist\三角洲口风琴_v0.12_内置解锁乐曲.exe`，同时更新标准构建输出 `dist\三角洲口风琴.exe`。SHA-256：`63c438428374666c226d21e3ba58cd05a79ab0a7016a7254f5bbc4337e770de4`。

## 2026-09-12：Windows 简谱保存按钮不可见修复

- 在用户正在运行的 v0.12 窗口确认：「新建简谱」默认尺寸下只有输入框，底部未显示保存按钮。原因是 Tk 的 pack 按顺序分配空间，默认 24 行的文本框先占满剩余高度，后加入的保存按钮被挤掉。
- 保存按钮改为在文本框之前从底部预留空间，输入区随剩余高度伸缩；保持曲库文件格式、保存后选曲和播放操作不变。
- 新增回归用例先在旧布局下复现 9 组失败，再在修复后通过：Tk 字体缩放 4/3、2、8/3（对应 96、144、192 DPI）与 720×510、660×480、900×700 三组尺寸。检查按钮可见且高度完整、输入框不遮挡按钮，并验证保存真实 JSON 后关闭弹窗、选中新曲目、生成播放计划且不自动演奏。
- `python -m unittest -v test_song_settings`：15 项通过；`python -m unittest -q test_music test_transport`：76 项通过，共 91 项。
- `python app.py --smoke --data-dir .\build\smoke-jianpu-save` 通过，42 个示例音符、主界面控件、托盘和隐藏恢复均正常。使用独立临时曲库检查默认尺寸下的[修复截图](docs/jianpu-save-fixed.png)，右下角保存按钮完整显示。
- 使用 PyInstaller 6.11.1 按既有 onefile、windowed、uac-admin 参数打包，成品为 `dist\三角洲口风琴_v0.12_简谱修复.exe`，SHA-256：`e186d95879b03948c1630a51e4abf7e30a9734c2cac9c1b808108c15bf8b05b4`。构建有 `pkg_resources` 隐式导入缺失警告，构建成功；本轮启动冒烟及截图验证针对源码，未启动带管理员清单的成品。
- 本轮修改仅涉及 Windows 简谱弹窗布局，未进行游戏内发键验证，未更新线上下载或版本目录。

## 2026-09-13：安卓 v0.5 日常停止保留无障碍授权

- 用户确认每次需要重设的是无障碍服务开关。检查发现旧主界面「停止并关闭演奏服务」调用 `disableSelf()`，会关闭系统开关。日常操作改为「停止并隐藏悬浮窗」，执行停止归零和隐藏，保持授权及待机服务；下一次恢复悬浮窗或正常退出后重开应用，不再主动撤销授权。
- 按实际系统开关与服务连接分别展示未授权、已授权等待连接、已连接三个状态；返回主界面立即刷新。未授权时点击「显示悬浮控制条」直接提供说明和系统设置入口，已有授权时使用「管理无障碍授权」，避免重复说明。跳转入口不可用时给出手动设置提示。
- 首次授权仍由用户在安卓系统页面确认，不能由普通权限弹窗自动完成；管理入口使用公开的 `ACTION_ACCESSIBILITY_SETTINGS`。触摸接口严重故障时原有断开服务机制保留；系统主动关闭或用户撤销后仍需重新开启。[Android 官方生命周期与 disableSelf 说明](https://developer.android.com/reference/android/accessibilityservice/AccessibilityService#disableSelf())。
- 更新主界面版本和授权说明，并修正系统无障碍服务描述中旧的「不联网」文字，明确线上曲库会下载目录及 MIDI、不会上传本地曲谱和设置。Manifest 未增加权限；仍只有 INTERNET 使用权限和无障碍服务的系统绑定保护。
- `android/build.ps1` 完整执行 `clean testDebugUnitTest assembleDebug lintDebug` 通过；22 项单元测试全部通过（核心 16、线上曲库 6），Lint 0 错误、21 警告。最终设备播放回归 15 项通过，覆盖校准、倒计时、变音、暂停停止释放、收起禁用及前台／方向保护，返回 `INSTRUMENTATION_CODE: -1`，记录位于本机 `work/android/v05-gesture-result.txt`。
- 授权设备入口 `-e suite permissions` 的 5 项检查通过：真实停止隐藏后系统开关与服务保持、恢复悬浮窗及主界面退出重开不重复申请、模拟连接未完成时正确展示已授权状态、管理授权发出系统设置 Intent、实际撤销后按需显示说明并发出设置 Intent。系统设置 Intent 由测试监视器截获，不声称验证了各厂商设置界面的具体布局。测试恢复原服务设置；正式 APK 不包含测试修改系统设置的能力。
- 前期设备测试的 Intent 监视器误匹配没有 action 的重开应用 Intent，导致测试等待；测试启动补充显式 `ACTION_MAIN` 后解决。终止该次测试时执行强行停止，模拟器将无障碍开关清空，下一次运行停在服务准备检查；重新设置专用模拟器后验证通过。这两次未计为成功。正常停止隐藏和主界面退出重开均有独立通过结果，不能外推为强行停止或所有厂商后台清理均保留授权。
- 权限界面截图检查：`android/docs/permissions-connected-v05.png`、`permissions-needed-v05.png`，分别展示已连接／管理授权和未授权／开启入口；测试截图等待前一步 Toast 消失后重新保存。线上曲库与 v0.4 音区切换代码未改，保留已有曲库及 12 点校准；本轮未重复网络设备测试和切换性能测量。
- 交付 `dist/三角洲口风琴_安卓_v0.5预览版.apk`，66,492 字节，版本 `0.5.0-preview`／5，APK v2 签名通过；构建、交付及模拟器实际安装包 SHA-256 一致：`ef86a6a3697de07f19aa27a6d0ed668de10c2ea4562e63c57018426fff688970`。所有 dex 检查包含授权状态与按需申请代码、线上曲库和 v0.4 时序逻辑，不包含 `GestureSmokeTest`、`permissionChecks` 或 `WRITE_SECURE_SETTINGS`。
- 本轮设备为自建 Pixel 6 / Android 14 模拟器，未连接真实手机，未向手游输入。真机系统设置入口及厂商后台策略仍需验证。手机端源码、说明、截图和提交继续仅保存在本地，不推送 GitHub。

## 2026-09-12：安卓 v0.4 减少音区切换停顿

- 原实现到音符开始时才切换音区，并在每次切换时完全暂停曲谱时钟。现改为倒计时、休止符及前一音符松键后预选下一音符所需音区；先利用已有空隙，切换超过下一音符开始时刻才限制时钟，避免短音被吞掉。倒计时内只预选变音，不提前按音键。
- 保留每次 45 毫秒点击，将两次点击之间及结束后的等待由 20 毫秒缩短到 8 毫秒。需要改变音区和半音时，使用一个包含两次顺序点击的手势批次，减少系统回调往返；三选一音区与独立半音规则不变。只有成功完成且播放批次仍有效时才更新状态，中断后重新确认半音。
- 指法在开始／继续前计算，悬浮窗只在文字或按钮状态改变时更新。保留播放及倒计时中禁用「收起」、暂停／停止后释放触摸、目标窗口及旋转检查。已有曲库、线上曲库和 v0.2／v0.3 的 12 点校准兼容；Windows 代码未修改。
- 同一 Pixel 6 / Android 14 模拟器、自建测试键盘、120 BPM／一倍速，以实际 `MotionEvent` 的 DOWN／UP 时间比较 v0.3 后端与最终 v0.4。累计延迟取最后一个音符与首音的实际按下间隔减谱面间隔，排除启动倒计时；空隙为上一音符 UP 到下一音符 DOWN。结果是单次设备测量，受调度影响，不是游戏音频测量。

| 测试曲段 | v0.3 累计延迟 | v0.4 累计延迟 | v0.3 平均空隙 | v0.4 平均空隙 |
| --- | ---: | ---: | ---: | ---: |
| `1 +2 1 -2 1 +2 1 -2 1` | 632 ms | 477 ms | 115 ms | 84 ms |
| `1 0 +2 0 -2 0 1` | 241 ms | 2 ms | 612 ms | 527 ms |
| `1 +#2 -#1 #1 2` | 395 ms | 292 ms | 131 ms | 97 ms |

- 连续跨音区平均空隙减少约 27%；休止符测试的空隙包含谱面指定的 500 毫秒休止，不能将其视为全部卡顿。第一轮 v0.4 连续跨音区测得 455 毫秒累计延迟、82 毫秒平均空隙，最终包复测如表；仍有系统点击与回调成本，不承诺零停顿或精确还原原速。基线、初测、最终记录分别保存在本机 `work/android/v04-baseline-timing.txt`、`v04-improved-timing.txt`、`v04-final-timing.txt`。
- `android/build.ps1` 完整执行 `clean testDebugUnitTest assembleDebug lintDebug` 通过。22 项单元测试通过（核心 16 项、线上曲库 6 项），新增覆盖提前切换时休止正常前进、超过截止点才等待、暂停停止后恢复时钟、倒计时预选及顺序变音批次。Lint 为 0 错误、21 条既有类别警告。
- 最终安装包的 15 项触摸回归和 2 项时序检查通过，均返回 `INSTRUMENTATION_CODE: -1`：12 点校准、半音确认、倒计时预选不发音、长按／重复音／休止、暂停与停止释放、变音中暂停及旧回调失效、收起禁用、前台／方向保护，以及二倍速下五个密集跨音区半音不丢音、不叠按、不取消。时序检查还断言音符不提前、充足休止时不累积切换延迟。记录见本机 `work/android/v04-final-gesture.txt` 和 `v04-final-timing.txt`。线上页面无变更，本轮未重复官网网络设备测试。
- 检查 `android/docs/collapse-disabled-v04.png`，悬浮状态、播放进度、置灰「收起」及测试键盘长按状态显示正常。
- 交付 `dist/三角洲口风琴_安卓_v0.4预览版.apk`，65,648 字节，版本 `0.4.0-preview`／4，APK v2 签名通过；构建、交付及模拟器实际安装包 SHA-256 一致：`770724776ebf5e8daddc3fb5017c2ebf716c07026cb92865bd06d3b712545b84`。所有 dex 合并检查包含预选与限时钟逻辑、线上曲库页面，不包含设备测试入口。`aapt` 直接使用中文 APK 路径时报编码错误，改在构建输出目录使用 ASCII 文件名后成功确认版本。
- 未连接真实安卓手机，测试仅向自建键盘发触摸；手游对缩短切换等待的响应、实际音高与听感仍需真机验证。手机端源码、文档、截图和提交仅保存在本地，未推送 GitHub。

## 2026-09-12：安卓 v0.3 线上曲库与收起禁用

- 新增线上曲库页面，沿用官网 `https://aiygzn.top/melodica/songs.json`，支持按曲名／作者搜索、每页 10 首、查看详情及下载并选用。下载成功后返回主界面选中本地曲目，同一线上标识重复选用不产生副本；已有 v0.2 的 12 点校准保持兼容。
- 网络请求在后台运行，仅接受 HTTPS，限制目录 1 MB、MIDI 10 MB、重定向次数与连接／读取时间；核对目录中的大小和 SHA-256，并解析 MIDI 后才原子保存。取消或写入失败清理临时文件；页面关闭后旧回调不能改变选曲。增加 INTERNET 权限并同步界面说明，不上传本地曲谱、设置或窗口信息。
- 按用户明确要求，倒计时及演奏状态下「收起」立即置灰禁用，操作入口再次检查状态。暂停／停止完成触摸释放后，以及曲目结束后恢复；手动触摸导致系统先取消演奏手势时，短暂保留禁用状态，避免同一次点击把悬浮窗收起。
- `clean testDebugUnitTest assembleDebug lintDebug` 完整构建通过。19 项单元测试全部通过（原核心 13 项、线上曲库 6 项），覆盖目录字段／重复标识／URL、中文搜索、大小与哈希、无效 MIDI、数据量／超时／取消、本地稳定标识及原曲保留。最终 Lint 为 0 错误、21 警告，主要为平台版本、界面本地化和既有触摸可访问性建议。
- Pixel 6 / Android 14 模拟器的 15 项触摸设备回归通过，返回 `INSTRUMENTATION_CODE: -1`；新增检查倒计时不能收起、播放结束及暂停后恢复、演奏中实际点击禁用按钮不会收起、停止后可以收起。原有变音、长音、暂停续播、窗口／方向检查全部通过。
- 线上曲库设备入口 `-e suite online` 的 5 项检查通过，返回 `INSTRUMENTATION_CODE: -1`：官网目录和当前 5 首 MIDI 均完成真实 HTTPS 下载、大小／SHA-256 校验及安卓解析；搜索与 27 首测试数据分页；取消／下载／返回主界面；已下载标记和重复选用；在目录请求未完成时返回。测试仅清理本轮新增的测试下载，恢复原有设置。
- 检查 `android/docs/main-v03.png`、`online-catalog-v03.png`、`collapse-disabled-v03.png`：线上曲库入口、曲名长文本、分页按钮和演奏期间置灰的「收起」显示正常。设备结果保存在本机 `work/android/v03-online-result.txt` 和 `work/android/v03-gesture-result.txt`。
- 交付文件 `dist/三角洲口风琴_安卓_v0.3预览版.apk`，65,140 字节，版本 `0.3.0-preview`／3，APK v2 签名通过；构建、交付及模拟器安装包 SHA-256 一致：`a556fc2fdbe13ef54d4c08040735c5298ef656886ee935231cb31e5bf1ff6551`。线上页面与收起保护已打包，测试入口未混入交付 APK。
- 未连接真实安卓手机，本轮输入仅面向自建测试键盘；仍需用户在手游中确认演奏听感和厂商系统行为。手机端源码、说明、截图和提交仅保存在本地，未推送 GitHub。

## 2026-09-12：安卓 v0.2 点击选中式变音

- 根据用户提供的手游截图及操作说明，校准改为八个音键加半音、升调、自然音、降调，共 12 处。旧校准明确失效，曲库和其余设置保留；校准标记按文字宽度居中显示。
- 输入改为先释放音键、串行点击变音按钮、再按住音键。升调／自然音／降调互斥，半音独立；相同状态不重复点击，选自然音不会顺带取消半音。沿用升降一个八度、半音升半音的音高映射。
- 开始和续播前确认半音实际选中状态；未经确认不发键。变音点击的旧回调不能恢复已暂停的播放或过期状态。切换过程中暂缓曲谱时钟，避免短音被吞掉；频繁变音会增加切换停顿。
- `android/build.ps1` 完整清理构建、13 项核心测试及 `lintDebug` 通过，Lint 为 0 错误、16 警告。新增核心回归覆盖三音区／半音组合、状态确认与失效、变音切换期间暂停／续播／停止的时钟行为。
- Pixel 6 / Android 14 模拟器的 `GestureSmokeTest` 共 13 项通过，返回 `INSTRUMENTATION_CODE: -1`。覆盖实际触摸完成 12 点校准且不穿透、旧校准禁止播放、半音未确认不发键、重复音／休止／连续长按、暂停释放和位置保持、续播／停止归零、取消倒计时、三音区和半音组合且不重复切换、半音初始已选中、变音点击中暂停后正确同步、密集跨音区短音、目标应用不匹配、旋转不匹配、切出暂停释放。完整结果保存于本机 `work/android/v02-gesture-result.txt`。
- 组合变音实际接收到 MIDI 音高序列 `48,49,51,61,63,75,78,74,60,60`；只点击 `降调、半音、自然音、升调、半音、自然音`，没有在音键按住时切换变音，没有触摸取消。125 毫秒的跨音区半音序列 `60,75,49,61,62` 未丢音。
- 截图复查时有一次设备测试在上一首的 UP 事件后立即切歌，撞上尚未结束的系统回调，触发应用「正在释放触摸」保护；该次失败未计为通过。测试入口已补齐停止后等待触摸完成回调的条件；界面截图也等待校准提示消失再保存。
- 检查竖屏设置／升级提示、横屏 12 点校准和半音确认截图，保存于 `android/docs/*-v02.png`。截图仅由设备测试入口保存；应用正常演奏不截图。
- 交付文件：`dist/三角洲口风琴_安卓_v0.2预览版.apk`，54,528 字节，版本号 `0.2.0-preview`／2，APK v2 签名验证通过。构建、交付和模拟器安装包 SHA-256 一致：`9e14cc0fd2189a8e856b4c3c01822d3858b39c67ba3d17a0037809d67b3608e1`。
- 本轮只向自建测试键盘发送触摸，未连接真实安卓手机；手游对按钮点击时长／间隔的响应、实际音高和演奏听感仍需真机验证。全部手机端源码、文档、截图及提交仅在本地保留，不推送 GitHub。

## 2026-09-12：安卓 v0.1 预览版

- 新增独立 `android/` 工程：Android 8.0+、MIDI 0／1 与简谱导入、本地曲库、音轨选择、速度及移调、八键与可选变音键校准、无障碍悬浮演奏。Windows 程序代码和本机曲库未修改。
- 本机构建环境：Android Studio 自带 JDK 21.0.9、Gradle 9.2.1、AGP 9.0.1、Platform 34、Build Tools 36.0.0。`android/build.ps1` 的完整清理构建、10 项核心单元测试、`assembleDebug`、`lintDebug` 通过；Lint 为 0 错误、17 警告，主要涉及界面文本本地化、触摸控件可访问性和平台建议。
- Android 14 / API 34 的 Pixel 6 模拟器完成安装、主界面、简谱／音轨控件、全屏八键校准及悬浮窗截图检查；截图保存在 `android/docs/`。
- 无障碍服务向自建测试键盘完整演奏《小星星》：42 次按下、42 次抬起、0 次取消、0 个残留按住音键。普通四分音符约 565～575 毫秒，最后一个两拍长音 1169 毫秒；这些是触摸时长，不是游戏音频听感。
- `GestureSmokeTest` 设备回归输出 `INSTRUMENTATION_CODE: -1`：重复音／休止／连续长按、暂停释放与位置保持、续播及停止归零、取消倒计时、目标应用不匹配保护、旋转校准检查、切出测试窗口暂停释放，共 7 项通过。测试仅操作自建键盘，结束恢复原校准和曲库选择。
- 验证中修复：完全静止的连续手势被系统合并为空事件，第二段回调取消；改为键心附近往返 1 个像素维持长按，并在结束时发送同一触摸序列的抬起。点击悬浮暂停时系统可能先取消手势，新增短暂继续抑制，避免一次点击又恢复演奏。
- 构建问题已处理：Windows 中文路径下测试进程参数文件与 Java 编码不一致，使用 JDK 21 和 `file.encoding=COMPAT`；增量打包曾残留旧 dex，交付脚本改为完整清理构建。最终核对构建包、交付包和模拟器已安装 APK 的 SHA-256 完全一致，包含长按修复且没有临时手势跟踪日志。
- 测试工具限制：普通 `uiautomator dump` 会抑制无障碍服务，导致演奏中断；设备回归使用不抑制服务的 UiAutomation 模式，并重连原本已开启的本应用服务。前期受这些工具／构建问题影响的运行没有计入通过结果。
- 交付文件：`dist/三角洲口风琴_安卓_v0.1预览版.apk`，51,220 字节，APK v2 签名验证通过。SHA-256：`9c77b31794779bed66b5279f97ee2296cc579fa526de68129afa0ad448b3d5c9`。
- 仍需安卓真机及三角洲手游验证：游戏是否接受无障碍手势、键位及中央音高、变音是按住还是切换、多指变音、游戏长音听感、密集音符、锁屏／后台限制、厂商系统兼容性。未连接真实手机，未向三角洲手游发出触摸，不将模拟器结果作为手游兼容性结论。

日期：2026-09-11。环境：Windows 11 x64、Python 3.13.5。

## v0.10 删除曲库曲目

- `python -m unittest -q test_music test_transport test_song_settings test_lifecycle`：92 项通过；新增删除确认取消、内置示例保护、曲库路径边界、文件删除和每曲设置清理回归测试。
- 删除只作用于 `songs` 目录中的程序副本，不删除原始 MIDI；删除后曲库自动刷新，内置示例始终保留。
- 源码冒烟检查继续通过；本次改动未改变播放和片段调度逻辑。
- PyInstaller 6.11.1 已生成 `dist\\三角洲口风琴_v0.10.exe`（21,801,807 字节），沿用管理员清单。

## 当前版本：演出片段设置导出

- 演出片段导出实现暂不在界面展示，保留相关回归覆盖，后续可按需重新开放入口。
- 新增 `test_song_settings.py` 回归测试，覆盖未保存片段的导出内容及导出后不改变当前已保存配置。

## v0.9 播放中选取片段

- `python -m unittest -q test_music test_transport test_song_settings test_lifecycle`：90 项通过；新增测试覆盖播放中打开片段面板、记录当前起止位置、暂停、片段面板进度拖动及保存。
- 播放试听时可以直接打开片段面板；面板实时跟随原曲位置，暂停后拖动进度条仍共用播放器游标。游戏演奏打开面板会先暂停并释放输入，继续时返回游戏焦点。
- 源码冒烟检查通过：`build\\v0.9-smoke\\smoke-result.json`；片段窗口控件边界检查通过。
- PyInstaller 6.11.1 已生成 `dist\\三角洲口风琴_v0.9.exe`（21,800,167 字节），沿用管理员清单。
- 本阶段自动化播放使用模拟输出，没有向真实游戏发送演奏按键；游戏内片段听感仍需人工试奏。

## 1.75 倍速

- 速度下拉框、悬浮窗速度按钮及 F10/F11 档位已加入 1.75 倍；每曲设置恢复也接受该值。
- `python -m unittest -q test_music test_transport test_song_settings test_lifecycle`：89 项通过，新增 1.75 倍速档位及边界回归测试。
- 原 `dist\\三角洲口风琴_v0.8.exe` 被 Windows 占用，未覆盖；新版输出为 `dist\\三角洲口风琴_v0.8_1.75倍速.exe`（21,797,166 字节）。

## v0.8 多片段演奏与每曲设置记忆

- `python -m unittest -q test_music test_transport test_song_settings test_lifecycle`：88 项通过；覆盖 MIDI 片段裁切、片段顺序与重复、跨片段松键、暂停续播、定位、改速、F9 归零、曲目参数切换／重启恢复、同名 MIDI 隔离及配置读写异常。
- 片段按 MIDI 原曲时间保存，最多 100 段，可添加、更新、删除、上下移动、重复或清空恢复全曲；片段跨越长音时只演奏被选中的部分，片段之间重新触发同音也会先释放旧键。
- 源码冒烟检查通过：独立数据目录下主窗口、托盘、隐藏／恢复及示例曲目读取正常；结果为 `build\\v0.8-final-smoke\\smoke-result.json`。
- 主窗口 1120×850、1000×830，以及片段设置窗口 720×620、680×600 完成自建 Tk 截图和控件边界检查；截图位于 `work\\v0.8-ui`，四个尺寸均通过。
- PyInstaller 6.11.1 已生成 `dist\\三角洲口风琴_v0.8_1.75倍速.exe`（21,797,166 字节），沿用管理员清单。自动化播放全部使用模拟输出，没有向真实游戏发送演奏按键；实际游戏听感和片段选择仍需人工试奏。

## 曲目参数记忆（第一阶段）

- `python -m unittest -v test_song_settings test_transport test_music`：67 项通过，其中新增 5 项覆盖切歌及重启恢复、同名 MIDI 的独立配置、损坏与无效配置回退、磁盘写入失败提示及原文件保留。
- 速度、移调、手动音轨与演奏方式在调整成功后原子写入独立的 `song-settings.json`，切歌时恢复；兼容没有此文件的旧数据目录。原曲库与全局键位设置位置不变。
- 本阶段全部播放输出使用模拟后端，没有向真实游戏发键；多片段编排与界面验证已在 v0.8 记录中完成。

## v0.7 播放中调速与移调

- `python -m unittest -v test_music test_transport test_lifecycle`：66 项通过，其中核心 49 项、播放控制 13 项、生命周期 4 项。输出全部使用模拟后端，没有向真实游戏发送演奏按键。
- 确认播放中改速不重新触发长音，按新倍率调整剩余时值和休止；移调先释放旧音，再从当前位置吹奏新音。覆盖连奏松键间隙、重复音、倒计时内调整、暂停后保留进度、F9 后继续调整仍保持归零，以及换音失败和焦点丢失后的释放。
- 通过真实 Tk 下拉框事件和后台事件队列验证主窗口、悬浮窗速度按钮、F4/F5 移调事件和 F10/F11 调速事件共用参数；播放批次及输出连接不重建，旧曲谱音符事件不会覆盖新音高。
- 模拟 Windows 消息循环验证四个新增热键的键码、禁止按住重复触发、增减方向、注册冲突提示和注销范围。尚未在真实游戏内验证新热键及听感。
- 源码冒烟检查通过：1120×850、托盘就绪、主窗口隐藏／恢复正常。独立数据目录下，1120×850 和 1000×830 的控件尺寸检查通过，速度／移调下拉框在播放期间可用，底部快捷键提示完整容纳。
- 原生悬浮窗 9 项测试在切换到自建靶窗口时受到 Windows 前台焦点限制，全部停在测试准备阶段，未计入通过项。截图工具首次返回被游戏遮挡的画面，重新选择预览窗口后激活失败（`failed to activate captured window`），未完成本轮可视截图检查；尺寸检查不代替截图或游戏实测。
- PyInstaller 6.11.1 已生成 `dist\三角洲口风琴_v0.7.exe`（21,786,528 字节），沿用管理员清单。成品以管理员权限通过独立数据目录的冒烟检查：42 个示例旋律音、1120×850 主界面、托盘就绪、隐藏／恢复正常，进程退出码为 0；未导入或读取个人 MIDI。
- 成品启动检查后尝试用管理员预览补查截图，用户按 Esc 停止了 Computer Use，已停止后续界面操作；截图与真实游戏热键、听感仍待人工确认。

## v0.6 暂停续播与进度拖动

- `python -m unittest -v test_music test_transport test_lifecycle`：54 项通过，其中核心 41 项、暂停与定位 9 项、生命周期 4 项。输出使用模拟后端，未向真实游戏发送演奏按键。
- F8 暂停后冻结播放时间并释放音键，再次播放从同一位置继续；覆盖倒计时暂停、长音中间续播、休止和提前松键间隙、曲尾及非法位置。
- 通过真实 Tk 进度条事件验证播放中拖动定位、松开后保持暂停，以及从所选音符继续；悬浮窗拖动共用同一播放位置。暂停时调整窗口大小不会令音符预览回到曲首。
- 覆盖拖动期间 F9 停止归零、旧停止事件不能覆盖新定位、快速续播忽略旧批次完成事件、F9 取消悬浮窗延迟启动，以及改速后进度归零。
- 源码冒烟检查通过：1120×850 主界面、播放按钮和底部提示布局、托盘就绪、主窗口隐藏／恢复正常。
- PyInstaller 最终成品 `dist\三角洲口风琴_v0.6.exe` 已以管理员权限启动并通过冒烟检查：8 个本地 MIDI 副本读取成功、托盘及窗口隐藏／恢复正常，退出码为 0。新版另存为版本文件；使用前从托盘退出旧版，再启动 v0.6。
- 原生悬浮窗 9 项复验受 Windows 前台焦点限制，测试在窗口切换处失败；未计入通过项。本地验证入口激活及主窗口截图重试仍受阻，工具返回 `failed to activate captured window`／`foreground window did not report a process id`，本轮未完成截图检查。既有 v0.5 实机记录仍保留，不能替代本轮验证。

## v0.5 音轨名称编码修复

- 定位《荒》的原始音轨名为 GBK 编码的“主旋律 伴奏”；原先按 Mido 默认 Latin-1 读取，产生西文乱码。《花海》使用同类中文编码，曲库中另有 UTF-8 编码的“口风琴”。
- 名称按各条原始字节分别解码，支持 UTF-8（含 BOM）与常见中文编码混用；解码后才去除首尾空白，保留无法可靠识别的原名称，不使用替换字符或忽略字节。
- 34 项核心测试通过，其中新增 5 项覆盖中文编码、混合编码与旋律推荐、西文名称保留、多字节尾字节和空名称回退。读取过程中不改写 MIDI。
- 对照修复前的 7 首本地曲目，SHA-256、原始音符数量、曲目时长、默认编译音符数量及播放时长均一致。《荒》仍为 351 个旋律音，《花海》仍为 486 个旋律音。
- 实际调用界面选曲逻辑，确认《荒》《花海》及 UTF-8 曲目的“自动旋律”标签和音轨下拉选项均包含正确中文，无需重新导入曲库。
- 修复版 EXE 以管理员权限完成冒烟检查，8 个本地 MIDI 副本读取通过，托盘及主窗口隐藏／恢复通过，退出码为 0。
- 打包时原 EXE 正在运行并被 Windows 锁定，因此修复成品另存为 `dist\三角洲口风琴_音轨修复.exe`。使用前从托盘退出旧版，再启动该文件。

## v0.5 悬浮窗显隐与托盘验证

- 29 项核心测试、9 项悬浮窗集成测试、4 项托盘生命周期测试通过，共 42 项。
- 通过主窗口的关闭协议隐藏主窗口后，向自建靶窗口发送真实 F6 热键，确认悬浮窗可反复显示／隐藏、主窗口保持隐藏、游戏靶窗口焦点不变，显示开关同步保存。F7 事件仍能打开操作面板；桌面上没有游戏窗口时也可正常开关面板。
- 窗口集成测试首次直接运行受到 Windows 前台焦点限制；通过本地验证入口获得焦点后，9 项全部通过。没有将首次失败计为通过。
- 真实创建系统托盘并通过菜单回调恢复主窗口、执行退出；隐藏主窗口不终止模拟播放，退出会释放按键、停止播放线程、关闭悬浮窗及托盘线程。F6 热键关闭后可重新注册。
- 托盘不可用时保留主窗口；模拟托盘运行故障时恢复主窗口并显示备用退出按钮。模态设置窗口不会因隐藏主窗口而失去入口；托盘启动后立即关闭也不会遗留线程。
- 源码和 PyInstaller 成品冒烟检查通过：主界面 1120×850，托盘就绪，主窗口隐藏／恢复通过，成品退出码为 0。成品沿用管理员启动，托盘运行所需的 pystray Windows 后端与 Pillow 已打包。
- 已检查悬浮观看窗截图，F6 显隐提示显示完整。主窗口预览期间窗口状态发生变化，未完成稳定的主窗口截图检查；主窗口控件和底部提示通过冒烟布局检查。
- 本轮自动化输入仅面向自建测试窗口，未向实际游戏发送演奏按键；游戏内音色与演奏效果不在本轮验证范围。

## 项目迁移与更名验证

- 项目根目录改为 `D:\projects\三角洲口风琴`。迁移前对源码及 Git 文件共 126 个文件逐一校验 SHA-256，新仓库保留原始提交历史；迁移前副本位于 `work\migration-backup`。
- 主窗口标题与标题栏、悬浮窗标题与面板名称、README、构建文件名统一改为“三角洲口风琴”。历史发行版本放在 `releases`，保留原文件名。
- 曲库与设置仍保存在 `%LOCALAPPDATA%\DeltaMelodica`，更名不会要求重新导入 MIDI。
- 在新目录中运行 29 项自动测试全部通过；主界面和悬浮窗完成截图检查，名称完整显示。
- 新打包文件为 `dist\三角洲口风琴.exe`，管理员清单检查通过。实际启动成品后日志显示“三角洲口风琴 v0.4”、管理员权限为 True，4 个示例 MIDI 与用户 MIDI 共 5 首通过冒烟检查，退出码为 0。
- 原工作目录已迁出，旧会话目录仅保留迁移说明与新位置指引。

## v0.4 增量验证

- 29 项测试通过，其中新增 8 项覆盖钢琴伴奏尾音不回填、错位和弦整理、持续高音与下行连奏、真实快速短音和重复音保留、短间隙连接与长休止保留、自动及手动音轨选择、完整时值和固定松键间隔、实际调度循环中的低音／半音／重复音释放顺序。
- 本机用户提供的《蒲公英的约定》原 MIDI 包含 1191 个原始音符、2 个钢琴音轨，总长约 250.907 秒。SHA-256 对照确认原文件未改变。
- 旧版全部音轨生成 615 段，其中 108 段短于 40 毫秒，最短约 1.894 毫秒。新版自动选择 Track 1 并使用钢琴连奏后生成 433 音，无短于 40 毫秒的音段，最短约 60.606 毫秒；没有音符重叠或负时值。
- 在所选 Track 1 内整理 102 个音段，连接 85 处短间隙；该曲无需八度折回。上述 102 不包含自动排除另一个音轨产生的变化，因此不能直接用它计算 615 到 433 的差值。
- 2:42～2:52 区间从 31 段变为 16 音，低于 C4 的伴奏碎音从 9 段变为 0 段；原有较长的乐句休止仍保留。
- 界面实际读取该 MIDI 验证默认自动旋律／连奏、手动切回全部音轨和旧版方式、演奏中锁定方式选择、设置保存按钮完整显示。1120×850 截图检查通过，1000×830 最小尺寸下底部说明完整显示。
- PyInstaller 发行 EXE 的清单包含 `requireAdministrator` 和 `uiAccess=false`。直接启动成品后日志确认管理员权限为 True，界面、4 个示例 MIDI 和用户 MIDI 的冒烟检查通过，进程退出码为 0。
- 本轮没有向实际游戏发送测试按键；以上确认的是曲谱转换、调度和程序界面。游戏内最终连奏听感仍需用户试奏确认。输入后端和悬浮窗交互沿用 v0.3。

下面为历史版本记录，v0.1 的待验证项目以较新版本的记录为准。

## v0.3 增量验证

- 21 项曲谱、按键、权限及播放调度测试通过。
- 6 项悬浮窗集成测试在管理员权限的本地测试窗口中通过：观看时保持原窗口焦点、真实鼠标点击穿透、面板选曲和焦点返回、播放前恢复焦点及穿透、透明度持久化和播放中锁定选曲、切到其他软件时隐藏。
- 普通权限测试进程曾因前台管理员游戏而无法切换测试窗口焦点；提升测试进程权限后全部通过，未将这次环境失败当成通过结果。
- v0.2 游戏内自动演奏和鼠标变音已由用户确认正常；v0.3 延续同一输入逻辑。悬浮窗的实际游戏显示效果仍需结合无边框／独占全屏模式确认。
- 发行 EXE 的嵌入清单已提取检查：`requireAdministrator`、`uiAccess=false`。直接启动 EXE 后日志确认管理员权限为 True，冒烟检查与 4 个示例 MIDI 读取通过。
- 440×452 的操作面板完成截图检查，文字与所有按钮完整显示；截图作为「悬浮窗预览.png」随包提供。

以下为 v0.1 的历史验证记录。

## 已验证

- 18 项自动化测试全部通过：八音键与逗号键、半音与八度组合、简谱时值、MIDI 跨音轨变速、打击乐过滤、单旋律整理、移调与音域折回、取消倒计时、停止清理、焦点切换、部分输入发送失败后的释放。
- 打包后的 EXE 启动成功，界面冒烟测试通过，退出码 0；实际读取并编译了 4 个示例 MIDI。
- 主界面在 1120×820 下完成截图检查；底部说明、播放器和八个音键均完整显示。
- 使用单独的本机自检窗口，通过真实 Windows SendInput 接收到了 Z、X、C、V、B、N、M、英文逗号，八个虚拟键码均一致。
- 本机 MIDI 合成器成功打开并关闭；F8／F9 全局热键注册成功。

## 自检发现

中文输入法启用时，本机自检窗口收到了 IME 转换事件和中文字符。仅在自检窗口临时停用输入法上下文后，八键全部正确接收，随后恢复上下文。发行版不会更改游戏输入法，需要用户在游戏内切到英文输入状态。

## 仍需游戏内验证

- 三角洲行动是否接收 Windows 模拟键鼠输入。
- 左／右键是否是按住降低／升高八度，中键是否是按住升半音，以及中央 1 的实际音高。
- 鼠标与音键组合在游戏中的响应、持续吹奏和实际节奏。鼠标组合的发送／释放顺序通过模拟后端测试，未在游戏内实际执行。
- 本机合成试听成功打开音源；听感和游戏内音色需要人工确认。

没有在游戏内自动演奏，也没有对游戏进程进行内存读取或修改。
# 2026-09-13 账号后端第一阶段

- `python -m unittest -v test_accounts`：12 项通过，使用临时 SQLite 与假邮件投递，未发送真实邮件。
- 覆盖邮箱验证、密码／会话摘要存储、验证码错误次数／过期／重放、登录、退出、重置撤销全部会话、Cookie 与来源检查、私有曲库隔离、游客批次归属、重试去重、请求大小及邮件失败。
- 尚待三端界面接入与验证；SMTP 实际投递和线上部署尚未执行。
